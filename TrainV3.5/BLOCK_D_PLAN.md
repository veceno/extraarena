# Block D — V5 League-2 (post-C consolidation) — PLAN

**Branch:** `glm-5.2/TrainV3.5Prep` (worktree `.claude/worktrees/glm-TrainV3.5Prep`)
**Pipeline position:** `Block -1 ✅ → 0 ✅ → A ✅ → B ✅ → C ✅ → D (this plan) → E1`.
**Status:** PLAN (solo-drafted from a 5-agent ultracode grounding fan-out; verify-only workflow RUN — FAIL→revised, 1 blocker + 2 majors + 9 minors merged surgically below). Execution = 3 in-worktree components D1–D3, dependency-ordered, synthetic-testable (FakeWorker / fake GameRunner / fake replay — no real MLX/Rust/ONNX required for tests, skip-gated where MLX needed). Block D is a LIGHT build block (mostly reuse of A4/B1/B5/B6/B7) — the load-bearing new code is the consolidation opponent mix (D1) + the `run()` override that emits the D→E1 handoff (D2); the per-update loop body (mix→parity→A4→curriculum→`_snapshot_step`) is COPIED from B8 (not inherited — B8 has no factored `_run_update` helper and `block_b_league_driver.py` is not editable, so D2 duplicates the inline loop at `:564-628` and keeps it in sync with B8 — divergence risk noted, regression guard `test_d2_loop_matches_b8_per_update_steps`).

**Spec anchor (design.md:130-131):**
- **Block D — League-2 (consolidation):** "Short self-play among {best post-C checkpoint, post-B/seed anchors, V4-orig spectrum, exploit-lanes} to settle post-C and prevent overfit to the last replay batch. Promotion by external bench."
- **Unified promotion = external-bench gate** (Rust `gauntlet_v5` + `run_v5_acceptance`), retargeted to best self-snapshot (`design.md:69`, D10) — the SAME bench Block B/C use; internal PPO loss/KL/entropy are monitoring only.
- **V4-orig's role in Block D** (`design.md:71`): a temperature-spectrum sparring lane (argmax-heavy), blind to mana_draw + new mechanics by design — V4-orig is a PROBE, not the bar.
- **mana_draw caveat** (`design.md:167`): mana_draw appears ONLY in human games (heuristic/model bots filter it) → Block D's self-play carries NO mana_draw training signal; Block D must not regress the mana_draw usage C3 established (monitored by the B6 mana_draw band + the B2/B8 collapse monitor if wired).

**Why Block D is a real BUILD (not just operational):** the spec's Block-D opponent SET {best post-C, post-B/seed anchors, V4-orig spectrum, exploit-lanes} maps cleanly onto the EXISTING B3 identity set (`self` = the post-C learner, `v5_snapshot` = post-B/peer anchors, `v4-orig-argmax/t07/t12`, `stall`/`anti_draw_greed`/`punish_empty_board`) — so no new dispatch identities and no A4 edit are needed. BUT B3's `build_block_b_opponent_mix` hard-codes a Block-B profile (self-snapshot prevalence CAPPED at 0.05, V4-orig 0.75, exploit 0.15, tail 0.05 — a weak-learner-vs-frozen-field league, `block_b_opponent_mix.py:126-157`). Block D is the OPPOSITE: the learner (post-C) is already strong, and the consolidation purpose ("settle post-C and prevent overfit to the last replay batch") wants self/peer-HEAVY prevalence so the post-C policy settles against its strong peers rather than grinding against a frozen field. The 0.05 cap (`SELF_SNAPSHOT_SHARE_CAP` `block_b_opponent_mix.py:157`) is the load-bearing mix blocker. So Block D needs a NEW `build_block_d_opponent_mix` (D1) that raises the self/peer prevalence and reweights the frozen field down — plus a `BlockDLeagueDriver` (D2) that overrides TWO surfaces in `BlockBLeagueDriver`: (a) `_build_reweighted_mix` (`block_b_league_driver.py:400-424`) to call D1's builder (+ `_merge_self_snapshot_split` so `v5_snapshot` is dispatchable), and (b) `run()` (`:550-633`) — the inherited `run()` builds a `BlockBLeagueManifest` (`:562`, a local var a subclass cannot redirect), early-returns on B7 `exit_fires` setting `exited_to_c2` (`:624-628`), and at schedule end sets `h2h_history` + `best_ever_path` only (`:630-633`); D2 must instead build `BlockDLeagueManifest`, suppress the B7 early-return under D-D3 fixed-schedule, and emit the schedule-complete `exited_to_e1`/`candidate_paths` D→E1 handoff. The per-update loop body (mix→B5 parity→A4→`curriculum.update`→`snapshot_cadence`→`_snapshot_step`) is COPIED from B8's inline loop (B8 has no factored helper; `block_b_league_driver.py` is not editable per §4, so D2 duplicates it — divergence risk noted). Plus a `c_to_d_handoff` (D3) that builds the Block-D seed pool from the C-loop's `best_ever_path`. Everything else — A4 live self-play, B1 pool/anchors/best-ever, B5 side-stratified parity, B6 4-component gate, B7 plateau detector — is reused verbatim.

---

## 1. Grounding summary (5-agent fan-out, citations verified against current code)

- **Pipeline / spec** (`design.md`): Block D is online self-play on the Online path (`design.md:164`), NOT the offline/human paths. "Promotion by external bench" = the unified Rust `gauntlet_v5`+`run_v5_acceptance` retargeted to best self-snapshot (`design.md:69`, D10) — same as B/C. The spec pins ZERO Block-D-specific hyperparameters (no N_snap/K_snap/duration/mix-weights restated for D); Block D's freedom comes from the spec pinning NO D-specific weights (Block B's weights at `design.md:118` ARE spec-pinned, not labelled tunable; only Block A's mix at `design.md:111` carries the explicit "(tunable)" label). E1 candidates: post-D, post-C3 (best C-iteration), post-B if C added nothing (`design.md:134`); ship = ONNX + sidecar + `_derive_kind_from_sidecar` + argmax prod deploy, marker `extra-lr-v5-max` (`design.md:158`). Integrity footprint for D: (i) online path only; (ii) exported checkpoint must pass the TIGHTENED V5 export-parity tolerance (`design.md:175`, the existing `LOGIT_TOL=1e-3` is too loose — a plan-time decision); (iii) deployed policy trips the ONNX fallback guard on regression (`design.md:174`); (iv) inherits Block -1 prerequisites (`design.md:79` Rust ArenaEnv parity; `design.md:177` rlhf_env codec sync).
- **`BlockBLeagueDriver` reuse** (`block_b_league_driver.py`): mostly reusable via config + caller setup, BUT D2 overrides TWO surfaces (not one). `__init__` (lines 299-354) injects `config`, `pool`, `game_runner`, `learner_policy`, `opponent_policies_factory`, `curriculum`, `parity`, `model`, `optimizer`, `checkpoint_namer`, `gauntlet_roster`, `h2h_opponent_kind`, + tunable knobs (`snapshot_cadence=2000`, `n_snap=5`, `k_snap=10`, `dominance_target=0.55`, `collapse_boost_factor=2.0`, `below_target_exits`, `min_gain`). `run(n_updates)` (lines 550-633) per-update: build mix → B5 parity feedback → A4 `run_live_self_play_update(opponent_mix_parsed=mix, p1/p2_score_rate=…)` → `curriculum.update(extract_lane_outcomes(rollout))` → every `snapshot_cadence` updates `_snapshot_step`. `_snapshot_step` (lines 470-547): measure (B5 side-stratified H2H + A5 gauntlet) → B6 `evaluate_block_b_gate` → B1 `set_seed_anchor` (FIRST) / `add_snapshot` + `maybe_update_best_ever` → B7 `detect_h2h_plateau`. **The inherited `run()` CANNOT be used verbatim by D2:** it constructs `BlockBLeagueManifest()` at `:562` (a local var a subclass cannot redirect), early-returns on B7 `exit_fires` setting `exited_to_c2=True` (`:624-628`), and at schedule end sets `h2h_history` + `best_ever_path` only (`:630-633`) — none of the `exited_to_e1` / `candidate_paths` / `block_d_schedule_complete` semantics D2 requires. So D2 overrides `run()` (a thin re-implementation that COPIES B8's inherited per-update loop body — B8 has no factored `_run_update` helper, the loop is inline at `:564-628`; `block_b_league_driver.py` is not editable per §4, so D2 duplicates the loop and keeps it in sync with B8 — divergence risk noted, regression guard `test_d2_loop_matches_b8_per_update_steps`) but builds `BlockDLeagueManifest`, suppresses the B7 early-return under D-D3 fixed-schedule, and emits the schedule-complete D→E1 handoff) AND `_build_reweighted_mix` (`:400-424`, which calls `build_block_b_opponent_mix` + `curriculum.reweight(cap=0.25)` + `_merge_self_snapshot_split(...)` — the merge is load-bearing: `v5_snapshot` is NOT dispatchable so without the merge A4 `resolve_opponent_dispatch` raises, see D2). The FIRST snapshot is forced to `set_seed_anchor` if `pool.seed_anchor is None` (lines 508-509) — so Block D's caller MUST pre-set the seed anchor (to post-C) BEFORE `run()`.
- **Pool / mix / gate / exit / C→D handoff** (`snapshot_pool.py`, `block_b_opponent_mix.py`, `block_b_gate.py`, `exit_to_c2.py`, `c_loop_driver.py`): `SnapshotPool` (B1) is seedable from an arbitrary checkpoint path via `set_seed_anchor(SnapshotEntry(path=...))` (`snapshot_pool.py:213-244`; seed immutable, becomes inaugural best-ever). `BLOCK_B_IDENTITIES` (`block_b_opponent_mix.py:105-117`) = the 11-name set covering the Block-D opponent set exactly. `build_block_b_opponent_mix` (`:276-325`) caps self-snapshot at 0.05 (`SELF_SNAPSHOT_SHARE_CAP` `:157`) and splits the self-snapshot budget equally between `self` and `v5_snapshot` (`_self_snapshot_split` `:261-273`) — the cap is the Block-D blocker. `evaluate_block_b_gate` (B6, `block_b_gate.py:315-436`) is PURE over measured rates + `aggregate_history` (4-component aggregate, monotone ≥ N_snap=5, promotion-by-loss guard — internal metrics NEVER read); reusable unchanged (C4 already reuses it, fresh-seeding `aggregate_history=[]`). `detect_h2h_plateau` (B7, `exit_to_c2.py:114-261`) is a pure function over an H2H series + `dominance_target` with a FLIPPABLE `below_target_exits` knob — the D→E1 firing branch is already encoded: with `below_target_exits=False`, `exit_fires = plateau and (not below_target)` with `reason='plateau_at_or_above_dominance_target'` (`exit_to_c2.py:232-236`); the `dominant_plateau_e1_path` reason (`:230-231`) is the NON-firing else-branch under the DEFAULT (`below_target_exits=True`) reading, NOT the firing path. So a D→E1 plateau exit reuses B7 with `below_target_exits=False` (no new code); the firing reason is `plateau_at_or_above_dominance_target`. C→D handoff: `CLoopDriver.run` returns `CLoopManifest` with `best_ever_path` + `exit_verdict` dict (`c_loop_driver.py:539-563`); NO typed handoff contract — Block D takes the path string and constructs its own seed anchor.
- **A4 live self-play + exploit identities + hyperparams** (`rust_live_self_play.py`, `ppo_phaseA_config.py`): `run_live_self_play_update` (`:1002-1160`) runs exactly ONE update (collect Rust rollout → `prepare_rust_ppo_batch` → optional PPO step); the multi-update loop is the driver's `run(n_updates)`. `opponent_mix_parsed` (`:1015`) bypasses `parse_v5_opponent_mix` and feeds a pre-parsed `[(name,weight)]` list straight into `sample_opponent_identities` (`:503-519`) — so Block D hands its consolidation mix the same way B8 does. `punish_empty_board` dispatches to Rust code 5 (`:147`, the B3 D-B10 uncomment, already enabled); exploit lanes `stall`=4 / `anti_draw_greed`=6 / `punish_empty_board`=5 (`RULE_AGENT_CODES` `:134-149`). All PPO hyperparams live on `PhaseAPPOConfig` (`ppo_phaseA_config.py:309-380`): `epochs=6`, `entropy_coef=0.01`, `clip_epsilon=0.2`, `value_coef=0.5`, `gamma=0.99`, `gae_lambda=0.95`, `minibatch_size=256`, `env_count=16`, `steps_per_update=30` — A3-frozen, inherited verbatim (D-B7 carry). NOTE: `max_turns=120` is NOT a `PhaseAPPOConfig` field — it is the module constant `PHASE_A_MAX_TURNS` threaded via `env_config` (`ppo_phaseA_config.py:376-379`); still A3-frozen, inherited verbatim. `lr` is external (wired into the MLX optimizer by the caller). `n_updates` and `steps` are driver/config-level knobs Block D can shrink freely without touching A3 frozen values.
- **Plan-doc format**: mirror Block B's fuller skeleton (numbered sections + `### Dn` components + fenced dependency graph + frozen-classic guard) with Block C's formal preamble + decision table + DECISIONS-CONFIRMED + Frozen/non-decisions paragraphs + plan-method note. `D-D*` decision IDs, `D1–D3` component IDs.

---

## 2. User decisions (genuine — present via AskUserQuestion AFTER this plan is verified)

| ID | Question | Default | Options |
|----|----------|---------|---------|
| **D-D1** | **THE Block-D opponent mix (load-bearing).** design.md:131 lists the SET {best post-C, post-B/seed anchors, V4-orig spectrum, exploit-lanes} but pins NO weights. B3's `build_block_b_opponent_mix` hard-codes a Block-B profile (self+v5_snapshot capped at 0.05, V4-orig 0.75, exploit 0.15, tail 0.05 — a weak-learner-vs-frozen-field league). Block D's consolidation purpose ("settle post-C, prevent overfit to the last replay batch") implies the OPPOSITE: the post-C learner is already strong and should settle against its strong peers (self/peer-HEAVY), with V4-orig spectrum + exploit-lanes as smaller PROBES (not the dominant field). The mix shape determines whether Block D is a real consolidation or just a re-run of Block B from the post-C checkpoint. | **Consolidation profile (self/peer-heavy)** — self+v5_snapshot ~0.50 (post-C learner + post-B/peer anchors dominating), V4-orig spectrum ~0.30 (argmax-heavy within-group 0.16/0.08/0.06), exploit-lanes ~0.15 (0.05 each), tail ~0.05. Spec-literal consolidation reading. | **Consolidation (self/peer-heavy ~0.50)** / Block-B-frozen-weights verbatim (0.05/0.75/0.15/0.05) / custom (user pins the 4 group shares) |
| **D-D2** | **Pool coexistence with Block D (Block-B open Q10, design.md:131/158).** Does Block D reuse the SAME Block-B `SnapshotPool`, a FRESH pool seeded from post-C, or EXTEND the Block-B pool with Block-D snapshots? The Block-B pool's seed anchor = the A-gate-PASS checkpoint and its best-ever = a Block-B snapshot; Block D's "best post-C" is a different checkpoint that should be the league's seed/anchor. | **Fresh pool seeded from post-C** — caller constructs a new `SnapshotPool`, `set_seed_anchor(post-C entry)` (post-C = immutable seed + inaugural best-ever) + `add_snapshot(post-B/seed anchor entries)` as peer non-anchors; the driver's first-snapshot `set_seed_anchor` branch is skipped. Cleanest: Block-D best-ever tracks Block-D improvement, not Block-B history. Resolves Block-B Q10. | **Fresh pool seeded from post-C** / reuse Block-B pool (post-C not the anchor) / extend Block-B pool (mixed history) |
| **D-D3** | **Block-D exit condition (D→E1).** Block B exits via B7 plateau→C2 (`below_target_exits=True`). Block D exits to E1. Two readings: (a) the spec says "short self-play" (design.md:131) with NO pinned length → a FIXED short schedule (run N updates, B6-promote at cadence, exit→E1 at schedule end carrying `best_ever`); (b) a plateau-based exit reusing B7 with `below_target_exits=False` (a dominant plateau AT/ABOVE the dominance target fires `reason='plateau_at_or_above_dominance_target'` → E1, `exit_to_c2.py:232-236`). | **Fixed short schedule** — Block D is "short self-play" by spec; run a small `n_updates` (sized for ~3-5 B6 promote checks), B6-promote at `snapshot_cadence`, exit→E1 at schedule end carrying `pool.best_ever.path`; NO plateau early-exit (a plateau early-exit is redundant for a short consolidation). | **Fixed short schedule (DEFAULT)** / plateau-based B7-flipped (`below_target_exits=False`) / both (schedule cap + plateau early-exit) |
| **D-D4** | **B4 curriculum reweight on/off for consolidation.** Block B oversamples losing lanes (`CurriculumReweighter.reweight(mix, cap=0.25)`, `block_b_league_driver.py:422`). Block D is a SHORT consolidation; curriculum reweight needs many samples to converge and a STABLE mix better serves "settle post-C." | **Off (stable mix)** — pass a no-op curriculum (cap=0.0 / identity reweight) so the consolidation mix is NOT per-lane-loss-reweighted across the short run; the mix shape is fixed by D-D1. (The mix is NOT byte-stable across updates even with curriculum off — `pool.self_snapshot_prevalence_weight()` grows as snapshots are added and `collapse_boost` varies with the learner's mana_draw rate — so "off" means "no per-lane-loss reweight," NOT "byte-identical mix.") | **Off / no per-lane-loss reweight (DEFAULT)** / on (reuse B4 per-lane-loss reweight) |

**Implementer defaults (surfaced for visibility, NOT user decisions):**
- **D-D5 — PPO hyperparameters:** inherit A3-frozen verbatim via the same `PhaseAPPOConfig` (this is a pure D-B7 carry — see Frozen/non-decisions below; NO separate D-D5 decision ID, NO re-litigation). `epochs=6`, `entropy_coef=0.01`, `clip_epsilon=0.2`, `value_coef=0.5`, `gamma=0.99`, `gae_lambda=0.95`, `minibatch_size=256`, `env_count=16`, `steps_per_update=30` (`ppo_phaseA_config.py:308-380`); `max_turns=120` is the module constant `PHASE_A_MAX_TURNS` (`:376-379`), NOT a `PhaseAPPOConfig` field; `lr` is external (wired into the MLX optimizer by the caller).
- **D-D6 — "Short" duration (n_updates / snapshot_cadence):** design.md:131 says "short" with NO number. The duration must be long enough for ~3-5 B6 promote checks (monotone over N_snap=5) to fire, short enough to be a consolidation not a fresh league. **Implementer default** — `n_updates` sized for ~3-5 snapshots (e.g. `snapshot_cadence` inherited ~2000 → `n_updates` ~6000-10000, or a smaller `snapshot_cadence` ~500-1000 → `n_updates` ~2000-4000 for a genuinely short consolidation). The closest prior-block analog is **D-B11** (Block-B compute budget ~20-40k updates, which WAS a user decision); Block-D "short" is implementer-defaultable where Block-B's open-ended budget was NOT because design.md:131 pins "short" (a magnitude constraint) for D while pinning no magnitude for B. Surfaced as a confirm-only item: the user may veto the "short" order of magnitude at plan-confirmation time (like D-B2's `min_gain` was surfaced), but it is not a load-bearing open question.

**Frozen / non-decisions (do NOT re-litigate):**
- **D10** — acceptance stays on Rust (`gauntlet_v5`/`run_v5_acceptance`); Python `model_benchmark` keeps legacy-H2H role only; no `V5Policy` adapter, no porting. Block D's "external bench" = the Rust gauntlet, unchanged.
- **D-B7** — A3-frozen PPO hyperparams inherited verbatim (the D-D5 implementer-default note above is this same carry — NO separate D-D5 decision ID, no re-litigation).
- **D-B1/B2** — `N_snap=5` / `K_snap=10` Block-B defaults; Block D reuses B6's `DEFAULT_BLOCK_B_N_SNAP=5` for its own promote gate (cross-block consistency), NOT re-pinned.
- **D-B3** — dominance-target / below-vs-above semantic; Block D's exit (D-D3) does NOT reuse the B→C2 below-reading (Block D exits to E1, not C2).
- **Promotion-by-loss guard** — B6's deliberate absence of internal-metric reads carries into Block D (the `BlockDLeagueDriver` inherits B6 unchanged).

> **Why D-D1 is THE user decision (do not resolve as an implementer choice):** the mix shape is the single knob that decides whether Block D fulfills its spec purpose ("settle post-C, prevent overfit to the last replay batch") or degenerates into a Block-B re-run from the post-C checkpoint. A frozen-field-heavy mix (B3 verbatim) keeps the post-C policy grinding against V4-orig it already beats (per C3's progression) — no consolidation, possible overfit to the frozen field. A self/peer-heavy mix makes the post-C policy settle against its own strong peers — the actual consolidation. The spec pins no weights and names this a "consolidation," so the high-level shape is genuinely the user's call; the exact numbers are implementer defaults under the chosen shape.

---

## 3. Components (dependency-ordered, synthetic-testable)

> **Component → spec-phase mapping (do not conflate):** plan components D1–D3 do NOT map 1:1 to a spec sub-phase — design.md:130-131 describes Block D as ONE phase ("League-2 consolidation") with no sub-phases. Mapping: **D1** = the consolidation opponent-mix builder (D-D1, load-bearing) · **D2** = the Block-D league driver (composes A4 + B1/B5/B6/B7, overrides `_build_reweighted_mix` + `run()`, emits the D→E1 candidate) · **D3** = the C→D handoff (builds the Block-D seed pool from `CLoopManifest.best_ever_path`, D-D2). A reader might conflate plan-D2 (the driver) with spec "Block D" (the whole phase) — they are distinct (D2 is the driver; the phase is D1+D2+D3 + reused A4/B1/B5/B6/B7).

### D1. `block_d_opponent_mix.py` — consolidation opponent mix (D-D1, load-bearing)
- `BLOCK_D_IDENTITIES` = the SAME 11-name set as `BLOCK_B_IDENTITIES` (`block_b_opponent_mix.py:105-117`): `{self, v5_snapshot, v4-orig-argmax, v4-orig-t07, v4-orig-t12, stall, anti_draw_greed, punish_empty_board, greedy_face, random, end_turn}`. NO new dispatch identities → NO A4 edit (the Block-D opponent set maps onto the existing identities: `self`=post-C learner, `v5_snapshot`=post-B/peer anchors, `v4-orig-*`, exploit-lanes, tail).
- `build_block_d_opponent_mix(pool, *, self_share_target=0.50, v4_orig_share=0.30, exploit_share=0.15, tail_share=0.05, collapse_boost=1.0) -> list[tuple[str,float]]` — mirrors `build_block_b_opponent_mix` (`block_b_opponent_mix.py:276-325`) BUT uses EXPLICIT group shares (honors D-D1's "custom" option) instead of B3's frozen 0.75/0.15/0.05 non-self budget. Asserts `self_share_target + v4_orig_share + exploit_share + tail_share == 1.0` (within 1e-6). The self+v5_snapshot share = `self_share_target` DIRECTLY (NOT via `pool.self_snapshot_prevalence_weight()` — that B1 method is the 0→0.05 ramp for B3's weak-learner regime; Block D's pool is pre-seeded with post-C + post-B anchors by D3, so the peer field is populated from update 1 and `self_share_target` applies as-is). The frozen field is split across the 3 groups by their explicit shares, preserving within-group ratios (V4-orig 0.40/0.20/0.15, exploit 0.05/0.05/0.05, tail 0.03/0.01/0.01). Self-snapshot split keeps `_self_snapshot_split` (`:261-273`) — half `self`, half `v5_snapshot`. Mix always sums to 1.0. `collapse_boost` (`:328-346`) available to further raise self-snapshot prevalence, capped at 0.95 (the B2/B8 mana_draw-collapse monitor hook). NOTE: D1 emits BOTH `self` and `v5_snapshot`; `v5_snapshot` is NOT dispatchable (absent from `POLICY_OPPONENT_KINDS`/`BLOCK_B_POLICY_OPPONENT_KINDS`/`RULE_AGENT_CODES`), so D2's `_build_reweighted_mix` override MUST wrap the result in `_merge_self_snapshot_split(...)` (see D2).
- **No separate `parse_block_d_opponent_mix`** — `BLOCK_D_IDENTITIES` is the SAME 11-name set as `BLOCK_B_IDENTITIES`, so `parse_block_b_opponent_mix` (`block_b_opponent_mix.py:223-255`) validates D1's names identically; re-export it as `parse_block_d_opponent_mix = parse_block_b_opponent_mix` for ownership clarity rather than re-implementing. (D2 feeds `opponent_mix_parsed=mix` directly to A4 on the runtime path, bypassing parse — the validator is only exercised in tests.)
- **NO edit to `block_b_opponent_mix.py`** (B3 is a completed, verified component — re-verification avoided). D1 is a NEW sibling module.
- **Depends on:** B3 within-group ratios (READ-ONLY constants), A4 `sample_opponent_identities` parsed-mix contract (`rust_live_self_play.py:503-519`, READ-ONLY). (Does NOT depend on `pool.self_snapshot_prevalence_weight()` — D1 uses `self_share_target` directly.)
- Tests: synthetic — mix sums to 1.0 at pool sizes {0, 1, 3, 6}; self+v5_snapshot share == `self_share_target` (exact, no ramp) and NEVER exceeds it; the group-share assertion fires on a bad sum; within-group ratios preserved; `collapse_boost` raises self-share capped at 0.95; the re-exported parser accepts the 11 names + rejects unknown + skips weight≤0; regression guard `test_block_d_mix_not_block_b_frozen` (D-D1 consolidation ≠ B3's 0.05 cap at a full pool).

### D2. `block_d_league_driver.py` — Block-D league driver (composes A4 + B1/B5/B6/B7; overrides `_build_reweighted_mix` + `run()`; emits D→E1 candidate)
- `BlockDLeagueDriver(BlockBLeagueDriver)` — SUBCLASS overriding TWO surfaces (the inherited `run()` cannot be used verbatim — it builds `BlockBLeagueManifest` and early-returns on B7 `exit_fires` setting `exited_to_c2`, `block_b_league_driver.py:562,624-628,630-633`):
  - **`_build_reweighted_mix` override** (`block_b_league_driver.py:400-424`): call `build_block_d_opponent_mix(self.pool, self_share_target=self.self_share_target, collapse_boost=...)`; when D-D4=off, skip the B4 per-lane-loss reweight (pass a no-op `CurriculumReweighter` with cap=0.0, or short-circuit `self.curriculum.reweight`); then **call `_merge_self_snapshot_split(mix)`** (reuse the B8 module-local helper at `:424`/`:670-700`, or import it) so `v5_snapshot` is merged into `self` BEFORE the mix reaches A4 — without this merge, A4 `resolve_opponent_dispatch` raises on `v5_snapshot` (absent from `POLICY_OPPONENT_KINDS`/`BLOCK_B_POLICY_OPPONENT_KINDS`/`RULE_AGENT_CODES`).
  - **`run()` override** (`:550-633`): a thin re-implementation that COPIES the inherited per-update loop body (B8 has NO factored `_run_update` helper — the loop is inline at `:564-628`; `block_b_league_driver.py` is not editable per §4, so D2 duplicates the loop body and keeps it in sync with B8 — divergence risk noted, with a regression guard `test_d2_loop_matches_b8_per_update_steps`). Only `_snapshot_step` (`:470-547`) and `_measure_snapshot` (`:427-467`) are genuinely inherited (called, not copied). The copied loop: `_build_reweighted_mix` → B5 parity feedback → A4 `run_live_self_play_update(opponent_mix_parsed=mix)` → `curriculum.update` → `snapshot_cadence`→`_snapshot_step` → manifest append → exit check. BUT (a) constructs `BlockDLeagueManifest` instead of `BlockBLeagueManifest`; (b) under D-D3=fixed-schedule, calls `detect_h2h_plateau` per snapshot for monitoring but does NOT act on `exit_fires` (suppress the inherited early-return at `:624-628`); (c) at schedule end sets `exited_to_e1=True`, `exit_verdict={'reason':'block_d_schedule_complete', 'n_updates_run':..., 'best_ever_path':pool.best_ever.path}`, `candidate_paths=[pool.best_ever.path, <post-C3>, <post-B>]`. Under D-D3=plateau, `exit_fires` with `reason='plateau_at_or_above_dominance_target'` (`exit_to_c2.py:232-236`) triggers the early exit (the flipped reading is wired via `below_target_exits=False` at `__init__`, see below — it is NOT a `run()` arg; the inherited `_snapshot_step:528` reads `self.below_target_exits`).
- **`BlockDLeagueDriver.__init__` additions** (over `BlockBLeagueDriver.__init__`): `self_share_target: float` (D-D1, default ~0.50); `exit_mode: str` ("fixed_schedule" vs "plateau", D-D3, default "fixed_schedule"); `curriculum_off: bool` (D-D4, default True); `e1_candidate_set: E1CandidateSet | None` (single-typed — the caller passes the D3 `E1CandidateSet` object; NO raw-string alternative, see D3). When `exit_mode='plateau`, pass `below_target_exits=False` to `super().__init__()` so the inherited `_snapshot_step:528` `detect_h2h_plateau` uses the flipped (at/above) reading; when `exit_mode='fixed_schedule'` the inherited default `True` is fine (`exit_fires` is ignored by the `run()` override anyway). `run()` fills `e1_candidate_set.post_d_path = pool.best_ever.path` at exit and derives `candidate_paths` from it.
- **`BlockDLeagueManifest`** — field split (NOT all cosmetic): INHERITED unchanged from `BlockBLeagueManifest` (`block_b_league_driver.py:202-231`): `update_metrics`, `snapshot_history`, `promotion_decisions`, `h2h_history`, `exit_verdict`, `n_updates_run`, `n_snapshots`, `best_ever_path`. RENAMED (cosmetic): `exited_to_c2` → `exited_to_e1`. NEW (not inherited — `BlockBLeagueManifest` has neither): `candidate_paths` (the E1 tournament set per design.md:134: post-D `best_ever_path` + post-C3 best + post-B fallback — Block D EMITS post-D; post-C3/post-B threaded from D3) and `aggregate_history` (FRESH-SEEDED at `run()` entry `[]`, like B8 `:488`/C4 — NOT carried from `CLoopManifest`; C's aggregate is the C3-replay bench context and mixing it into Block D's B6 monotone window would context-mix. Sized via D-D6 for ≥N_snap+2=7 snapshots so ≥3 B6 promote checks fire despite the fresh window). `to_dict()`.
- **Pool pre-seed requirement:** the caller MUST `pool.set_seed_anchor(post-C entry)` BEFORE `run()` so the inherited first-snapshot `set_seed_anchor` branch (`block_b_league_driver.py:508-509`) is skipped and post-C (not the first Block-D snapshot) is the seed anchor (D-D2). A `test_pre_seed_skips_first_snapshot_seed_anchor` regression guard.
- **NO edit to `block_b_league_driver.py`** (B8 completed/verified). D2 is a NEW subclass module (mirrors the B8→C4 per-phase-driver pattern: a new driver class per league phase).
- **Depends on:** B8 `BlockBLeagueDriver` (subclass, READ-ONLY inheritance) + `_merge_self_snapshot_split` (reused), D1 `build_block_d_opponent_mix`, D3 `E1CandidateSet`, B1 `SnapshotPool`, B5 `SecondStartParityLoop`/`play_side_stratified_gauntlet`, B6 `evaluate_block_b_gate`, B7 `detect_h2h_plateau`, A4 `run_live_self_play_update`, A5 `play_gauntlet`.
- Tests: synthetic (FakeWorker + fake GameRunner + fake opponent_policies, mirroring B8/C4 test injection) — D-D1 consolidation mix is built each update (spy on `_build_reweighted_mix`); **no `v5_snapshot` entry reaches `run_live_self_play_update` (the `_merge_self_snapshot_split` regression guard)**; D-D3 fixed-schedule exit fires at `n_updates` end with `exited_to_e1=True` + `exit_verdict['reason']=='block_d_schedule_complete'` + `best_ever_path` + `candidate_paths` (NOT a B7 plateau exit); D-D3-plateau variant exits with `exit_verdict['reason']=='plateau_at_or_above_dominance_target'` (NOT `dominant_plateau_e1_path`); **D-D4-off: spy on `curriculum.reweight` called with cap=0.0 (or no-op returns input unchanged) AND the mix handed to A4 == `_merge_self_snapshot_split(build_block_d_opponent_mix(pool, collapse_boost=...))` with no per-lane-loss reweight — do NOT assert byte-stability across updates unless pool prevalence + mana_draw rate are pinned to constants**; pre-seed skips first-snapshot seed anchor; B6 gate reused unchanged (NOT A5 a_gate — regression guard `test_uses_b6_not_a_gate`); B7 NOT used for exit under fixed-schedule (its `exit_fires` ignored, regression guard); `BlockDLeagueManifest.to_dict()` keys.

### D3. `c_to_d_handoff.py` — C→D handoff (build the Block-D seed pool from `CLoopManifest.best_ever_path`; thread E1 candidates)
- `build_block_d_seed_pool(c_manifest, *, post_b_anchor_paths=None, post_c3_best_path=None, h2h_vs_best=0.0, p1_p2_gap=0.0) -> SnapshotPool` — constructs a FRESH `SnapshotPool` (D-D2), calls `set_seed_anchor(SnapshotEntry(path=c_manifest.best_ever_path, update_number=0, h2h_vs_best=..., p1_p2_gap=..., promotion_eligible=True))` so post-C is the immutable seed + inaugural best-ever (`SnapshotEntry.update_number` is a REQUIRED field with no default, `snapshot_pool.py:82`; `role="seed"` is OMITTED — `set_seed_anchor` reconstructs the entry with `SEED_ROLE` internally, `snapshot_pool.py:230`, so passing `role="seed"` is a no-op that misreads the contract); then `add_snapshot(SnapshotEntry(path=p, update_number=0, role="rolling"))` for each post-B/seed anchor path (peer non-anchors). The post-C entry's `h2h_vs_best` seeds the best-ever H2H baseline.
- `E1CandidateSet` (frozen dataclass) — `{post_d_path: str|None, post_c3_best_path: str|None, post_b_path: str|None}` per design.md:134 (post-D filled by D2 at exit; post-C3 + post-B threaded through from the C→D handoff). `d2_manifest` carries it out as `candidate_paths`.
- `thread_e1_candidates(c_manifest, post_b_path) -> E1CandidateSet` — captures post-C3 (`c_manifest.best_ever_path`) + post-B for the E1 tournament; post-D is filled by D2's `run()` at exit.
- **NO typed C→D handoff contract exists** (`CLoopManifest` carries `best_ever_path` + `exit_verdict` dict only, `c_loop_driver.py:539-563`) — D3 is the BRIDGE that materializes the path into a `SnapshotEntry` + seed pool. This is the typed handoff Block D introduces.
- **Depends on:** C4 `CLoopManifest` (READ-ONLY, `c_loop_driver.py:186-214`), B1 `SnapshotPool`/`SnapshotEntry`/`set_seed_anchor`/`add_snapshot` (`snapshot_pool.py:67-265`).
- Tests: synthetic — `build_block_d_seed_pool` seeds post-C as the immutable seed anchor + inaugural best-ever (`pool.seed_anchor.path == c_manifest.best_ever_path`, `pool.best_ever.path == c_manifest.best_ever_path`); post-B anchors added as rolling non-anchors (FIFO-eligible, NOT anchors — INTENTIONAL: the `SnapshotPool` non-anchor slot count is bounded, and post-B peers are sparring partners for the short consolidation, not permanent anchors; they may be FIFO-evicted as Block-D snapshots are added, which is acceptable under D-D2 fresh-pool semantics); a second `set_seed_anchor` raises `RuntimeError` (immutability, `snapshot_pool.py:218-223`); `E1CandidateSet` carries post-C3 + post-B (post-D None until D2 fills it); a `CLoopManifest` with `best_ever_path=None` (C-loop skipped) is handled (raise or empty pool — implementer default, surfaced).

---

## 4. Frozen-classic guard (preserved, in effect — carries from Block A/B/C)

- **Prod runtime must not import TrainV3.5** — all D components live under `TrainV3.5/python/train_v3/`; no prod import.
- **Frozen-classic byte-frozen** — `classic_obs_v1/classic_actions_v1/classic_card_shape_v1/classic_rl_env.py/reward_v5.py` NOT touched; D1/D2/D3 import B1/B5/B6/B7/A4/A5 READ-ONLY.
- **`v5_trace.py` NOT modified + NOT imported** (Block D is online self-play, not the offline/human trace path).
- **`core/state.py` NOT modified**; `league_v5.py`/`gauntlet_v5.py`/`opponents_v5.py` consumed READ-ONLY.
- **`rust_ffi.py` NOT touched** (no Rust edit in Block D → cargo 157 unchanged, no re-confirmation needed); `punish_empty_board` already dispatches (B3 D-B10 uncomment, `rust_live_self_play.py:147`).
- **`block_b_league_driver.py` / `block_b_opponent_mix.py` NOT modified** (B8/B3 completed + verified) — D1 is a NEW sibling module, D2 is a NEW subclass; the B8 `_build_reweighted_mix` + `run()` surfaces are overridden (not edited), and `_merge_self_snapshot_split` is reused (imported, not redefined).
- **`rust_ppo.py` / `rust_live_self_play.py` NOT modified** — D2 feeds `opponent_mix_parsed=` the D1 mix the same way B8 does (bypassing `parse_v5_opponent_mix`).

---

## 5. Open questions / latent findings carried

1. **D-D1 (THE mix decision)** — the consolidation vs frozen-field shape is the load-bearing user decision; default = self/peer-heavy ~0.50. NOT an implementer choice.
2. **D-D2 (pool coexistence, Block-B Q10)** — fresh-seeded-from-post-C vs reuse/extend Block-B pool; default = fresh. Resolves Block-B Q10.
3. **D-D3 (exit condition)** — fixed short schedule vs plateau-based B7-flipped vs both; default = fixed short schedule.
4. **D-D4 (curriculum on/off)** — no per-lane-loss reweight vs B4 reweight; default = off (the mix is NOT byte-stable across updates even with curriculum off — pool prevalence grows + `collapse_boost` varies — so "off" means "no per-lane-loss reweight," not "byte-identical mix").
5. **Export-parity tolerance tightening (design.md:175, latent)** — the existing `LOGIT_TOL=1e-3` is too loose (a real V5 export bug <1e-3 slips). The TIGHTENED value is a plan-time decision; it lands on Block D's exported checkpoint (post-D → E1 tournament → ship). Flagged for E1 (the export/ship block), NOT a Block-D-self-play task — but Block D's emitted candidate must be export-valid under the tightened tolerance. Deferred to E1 planning.
6. **mana_draw regression risk (design.md:167, latent)** — Block D's self-play carries NO mana_draw training signal (bots filter it); a long consolidation could drift the candidate's mana_draw usage out of the human band. The B6 mana_draw-band gate component (`block_b_gate.py:366-373`, reuses A5 `check_mana_draw_band`) is the natural guard — a post-D checkpoint that regressed mana_draw FAILS B6 promotion. The B2/B8 mana_draw-collapse monitor (`collapse_reweight_boost`) is available via D1's `collapse_boost` if Block D runs long enough for collapse to matter. For a SHORT consolidation (D-D3/D-D6), B6's band is sufficient; no new component.
7. **E1 candidate threading (latent)** — design.md:134 lists 3 E1 candidates (post-D, post-C3 best, post-B if C added nothing). Block D EMITS post-D (`best_ever_path`); post-C3 + post-B are threaded from the C→D handoff (D3 `E1CandidateSet`). "if C added nothing" (post-B fallback) is an E1 selection rule, NOT a Block-D task — Block D carries post-B if the C→D handoff provides it. Deferred to E1 planning.
8. **`below_target_exits` cosmetic (latent)** — the inherited `BlockBLeagueManifest.exited_to_c2` field name is a misnomer for Block D (E1, not C2); D2's `BlockDLeagueManifest` renames it `exited_to_e1` (cosmetic, not functional — the boolean semantics are unchanged).

---

## 6. Dependency graph

```
Block A + B + C in-worktree COMPLETE (A4 rust_live_self_play, A5 a_gate, B1 snapshot_pool,
  B3 block_b_opponent_mix, B5 second_start_parity, B6 block_b_gate, B7 exit_to_c2,
  B8 block_b_league_driver, C4 c_loop_driver)
   |
   +-- D1 block_d_opponent_mix.py (consolidation mix: raises self/peer prevalence, same 11 identities, no A4 edit)
   |     depends: B1 SnapshotPool.self_snapshot_prevalence_weight (:418 RO), B3 within-group ratios (RO),
   |              A4 sample_opponent_identities parsed-mix contract (:503 RO), design.md:131, D-D1
   |
   +-- D3 c_to_d_handoff.py (C->D: fresh pool seeded from post-C + post-B peer anchors; E1 candidate threading)
   |     depends: C4 CLoopManifest.best_ever_path (:539 RO), B1 SnapshotPool/SnapshotEntry/set_seed_anchor (:213),
   |              design.md:134/158, D-D2; SIBLING of D1 (no D1 dep)
   |
   +-- D2 block_d_league_driver.py (BlockDLeagueDriver subclass overrides _build_reweighted_mix [+ _merge_self_snapshot_split] AND run(); BlockDLeagueManifest exited_to_e1 + candidate_paths + aggregate_history)
         depends: B8 BlockBLeagueDriver (:241 subclass RO) + _merge_self_snapshot_split (:424/:670 reused),
              D1 build_block_d_opponent_mix, D3 E1CandidateSet, B1 pool, B5 parity, B6 gate, B7 plateau (monitoring),
              A4 run_live_self_play_update, A5 play_gauntlet, design.md:130-131/164, D-D3/D-D4
```

DURABLE: write ASCII-only workflow scripts to `.claude_tmp_d{N}.js`, `node --check`, invoke via `scriptPath`, delete temp before commit.

## 7. Execution (ultracode, each: implementer + 4 refute-by-default verifiers + fix; independently re-confirm green; commit; push)

Solo-draft + verify-only for plan-writers (the stalling fix from Block B). This plan is solo-drafted from the 5-agent grounding fan-out above; the verify-only workflow (4 refute-by-default critics + Finalize) runs next to merge findings surgically BEFORE the D-D* user decisions are presented. Then each component D1→D2→D3 is implemented via the implementer + 4-verifier + fix ultracode workflow, independently re-confirmed green, committed, pushed. BLOCK_D_COMPLETION.md + transition to E1 (tournament + ship) after.

**No top-level Block-D runner component is built in-worktree (mirrors Block C, where C4 was the loop driver but the operational rlhf RUN was USER-run per D-C0/D-C9).** The operational wiring `D3.build_block_d_seed_pool(c_manifest) → BlockDLeagueDriver(pool=..., ...).run(n_updates) → d2_manifest` is USER-run glue (the league RUN, gated on the A-gate / C-loop PASS carrying a `CLoopManifest.best_ever_path`); D2 IS the per-update driver. If a thin `block_d_runner.py` wrapper is later wanted for repeatability, it is an E1-prep operational artifact, not a Block-D component — flagged here so the component set (D1/D2/D3) is not mistaken for incomplete.