# Block E1 -- V5 Tournament + ship (the FINAL V5-Max stage) -- PLAN

**Branch:** `glm-5.2/TrainV3.5Prep` (worktree `.claude/worktrees/glm-TrainV3.5Prep`)
**Pipeline position:** `Block -1 -> 0 -> A -> B -> C -> D -> E1 (this plan, the FINAL stage)`.
**Status:** PLAN (solo-drafted from a 6-agent ultracode grounding fan-out; verify-only workflow pending -- 4 refute-by-default critics + Finalize merge findings surgically BEFORE the E-E* user decisions are presented). Execution = 5 in-worktree components E1-E5, dependency-ordered, synthetic-testable (fake ONNX blob / fake InferenceSession / fake GameRunner / fake C2 client -- no real MLX/Rust/ONNX/rlhf_env required for tests, skip-gated where MLX/onnxruntime needed). E1 is a HEAVY build block relative to D: the load-bearing new code is (a) the V5 ONNX exporter (E1 -- a new torch mirror of the V5 split-encoder + mana_draw parallel head + a V5 weight map + a V5 sidecar, because the V4 `export_onnx.py` hardcodes obs_dim=1456 + a 2-tuple output and is structurally V4-frozen) and (b) the prod V5 deploy path (E5 -- `BerserkInference._validate_train_v2_contract` REJECTS obs_dim!=1456, so V5 cannot load through the existing prod bot brain without a new V5 format branch + mana_draw wiring). The tournament harness (E3) + the human-QA panel (E4) compose existing A5/B5/B6/A4/C2 surfaces; the export-parity test (E2) tightens the V4 `LOGIT_TOL=1e-3` and adds the mana_draw_logit parity axis.

**Spec anchor (design.md:133-189):**
- **Block E1 -- Tournament + ship** (`design.md:133`): "Candidates: post-D, post-C3 (best C-iteration), post-B (if C added nothing)." "Eval on the Rust ArenaEnv gauntlet (retargeted to best self-snapshot) + a human-QA panel in rlhf_env (operationalizes 'subjectively harder for humans')." "Ship: export best -> ONNX + sidecar; register via `_derive_kind_from_sidecar`; deploy to prod arena (argmax); marker `extra-lr-v5-max`. Snapshots/temperature variants feed the next iteration's league pool."
- **Two distinct strength notions** (`design.md:138-141`): (1) ABSOLUTE strength anchor -- vs frozen V4-orig (asymmetric: V5 advantaged, V4-orig blind to mana_draw + new mechanics), H2H >= 0.70 (the user's original 70-80% band retargeted to V4-orig); (2) PROGRESSION signal -- vs the immediately-prior best self-snapshot, a strong candidate wins only mildly (~0.52-0.55), monotone improvement detected by the plateau rule is the promotion driver. E1 MUST keep these two notions separate.
- **Final acceptance thresholds table** (`design.md:143-156`): H2H vs V4-orig >= 0.70; H2H vs Random ~1.0 (sanity); H2H vs end_turn ~1.0 (sanity); H2H vs best self-snapshot monotone ~0.52-0.55; no_bonus p1/p2/second (vs best self-snapshot) >= 0.70 each; no_assist >= 0.55 (calibrate up); exploit_resistance >= 0.50; p1_p2_max_score_gap <= 0.12; mana_draw usage in [0.5x, 1.5x] human baseline; min_entropy >= 0.70 / max_abs_kl <= 0.12; min_e2e_tps >= 12000; human-QA difficulty = subjective panel.
- **Integrity footprint** (`design.md:171-189`): skip orphan battles (`meta.status != 'finished'`, Block 0 carry); reconstruct GameState from `pre_state` (never `rng_seed`, Block 0 carry); ONNX fallback guard (`policy_factory` raises RuntimeError on silent rule-based fallback, `design.md:174` -- the last-resort PROD safety); tighten V5 export-parity tolerance (`LOGIT_TOL=1e-3` is too loose, `design.md:175`); warm-start parity (V5 601-scorer + base-1456 path bit-close to V4-Max immediately after load, `design.md:176` -- Q3 PARTIAL-relaxed to faithful-layer equality, see E2); rlhf_env codec sync (Block -1 carry); V5 ONNX<->MLX export within tightened tolerance (`design.md:189`).
- **Pipeline feeder** (`design.md:130-131, 60-66`): Block D is the E1 feeder; D emits `BlockDLeagueManifest.candidate_paths` = [post-D, post-C3, post-B] (Nones dropped, post-D first) via `E1CandidateSet` (D3 `c_to_d_handoff.py:61-86`, frozen). E1 CONSUMES this set verbatim.

**Why Block E1 is a real BUILD (not just operational):** three structural gaps the spec requires E1 to close:
1. **The V5 ONNX exporter does NOT exist.** `ai/train_v2/export_onnx.py` hardcodes `obs_dim=1456` (`:104,150,177`), a single `state_encoder` + 2-tuple `(logits, value)` output (`:19-65,164`), `MODEL_VERSION_ONNX='classic_action_conditioned_onnx_v1'` (`:16`), and a 5-group `_WEIGHT_MAP` (`:68-74`). V5 is `obs_dim=7128` with a split-encoder (base/global/private/history + state_fuser) + a PARALLEL `mana_draw_head` (`v5_policy.py:65-140`, `nn.Linear(hidden_dim,1)` at `:85`) + a 3-tuple `(logits, value, mana_draw_logit)` return (`:152`). The V4 exporter CANNOT export V5, and it is V4-frozen (pristine legacy, SPEC gamma) -- so E1 must author a NEW V5 exporter (E1) mirroring `v5_policy.py` in torch. The deploy adapter `V5RlhfAdapter._build_onnx_inference` (`v5_rlhf_adapter.py:104-125`) already expects the 3-tuple ONNX contract `session.run(['logits','value','mana_draw_logit'], ...)` -- the export target is fixed by the deploy adapter.
2. **The prod bot brain REJECTS V5.** `BerserkInference._validate_train_v2_contract` (`bot_brain.py:134-185`) asserts `obs_dim==1456` (`:145`), `action_feature_dim==171` (`:147`), `max_candidate_actions==601` (`:149`), and that the `logits` output is present (`:158-159` -- it does NOT assert `value` is present NOR that there are exactly 2 outputs, so a hypothetical 3-tuple ONNX with `logits` would PASS the output check); the real V5 barrier is the FORMAT gate (`profile_format != _TRAIN_V2_FORMAT='train_v2_classic_v1'` -> profile SKIPPED, `bot_brain.py:219-225`) + the `obs_dim==1456` check (`:145`). V5 (format `'v5'`, obs 7128) FAILS here and cannot load through the single prod bot brain (`web/server.py:5745 BERSERK_BRAIN = BerserkInference(profiles=BOT_DIFFICULTY_PROFILES)`). Shipping V5 to prod REQUIRES a new V5 format branch in `BerserkInference` + a `_get_action_v5` path that reads the 3rd output and wires `mana_draw_legal_mask` + `select_includes_mana_draw` (the helpers EXIST at `mana_draw_head_v5.py:76,116` but are CURRENTLY UNWIRED in any inference path). This is the largest E1 design choice (E-E2).
3. **No unified E1 tournament harness exists.** `run_v5_acceptance.py` is named by SPEC `:136` but (a) has a BROKEN `sys.path.insert(0, str(ROOT/'TrainV3'/'python'))` at `:16` (the worktree is `TrainV3.5` -- `a_gate.py:78-86` already bypassed it via direct relative import); (b) `_gauntlet_contract_gates` (`:288-309`) calls the SYNTHETIC `compare_adaptive_strength_monotonicity` proxy (`league_v5.py:146`, NOT real H2H games) and plays NO games vs a self-snapshot; (c) `_v4max_no_bonus_benchmark_gates` (`:312-380`) reads PRE-BAKED winrates from a `--v4max-no-bonus-benchmark` JSON, NOT live games. There is NO single entry that plays the full E1 candidate set vs {V4-orig, Random, end_turn, best self-snapshot} on the Rust ArenaEnv and emits the SPEC `:143-156` threshold table. E1 builds a NEW `e1_tournament.py` (E3) composing `a_gate.play_gauntlet` + `second_start_parity.play_side_stratified_gauntlet` + `opponents_v5` lane roster + the A4 `rust_live_self_play` runner (the production `GameRunner` wiring, `rust_live_self_play.py:311 SelfPrevOpponent` = best-self-snapshot, `:329 V4MaxOpponent` = V4-orig absolute anchor). The retargeting to best self-snapshot is ALREADY DONE at Block B (`block_b_league_driver.py:167 _DEFAULT_H2H_OPPONENT_KIND='best_ever'`) -- E1 REUSES it, does NOT retarget.

---

## 1. Grounding summary (6-agent fan-out, citations verified against current code)

- **Spec / pipeline / handoff** (`design.md:40,60-66,133-189`; `BLOCK_D_COMPLETION.md:90-108`): Block E1 = Tournament + ship, the FINAL V5-Max stage. Candidates = `E1CandidateSet{post_d_path, post_c3_best_path, post_b_path}` (`c_to_d_handoff.py:61-86`, frozen @dataclass, `with_post_d` the only mutation via `dataclasses.replace`) threaded by Block D as `BlockDLeagueManifest.candidate_paths` (`block_d_league_driver.py:137,153`, ordered [post-D, post-C3, post-B] Nones dropped). SPEC gamma frozen v1 codec: `classic_obs_v1` (1456), `classic_actions_v1` (601 candidates, 171 action features), `classic_card_shape_v1` byte-frozen -- V4-orig ONNX runs unchanged forever; V5 reuses the 601/171 candidate block UNCHANGED and adds a PARALLEL `mana_draw` binary head (NOT a 602nd candidate, `v5_policy.py:79-85`). Ship = ONNX + sidecar + `_derive_kind_from_sidecar` registration + prod arena argmax deploy + marker `extra-lr-v5-max`. The D-league RUN is USER-run operational glue (the prior-block pattern, `BLOCK_D_COMPLETION.md:106`); E1 follows the same split -- in-worktree components synthetic-tested, the operational tournament+ship RUN USER-run. D-D6 "short" duration is a confirm-only implementer default carried into E1-prep (`BLOCK_D_COMPLETION.md:107`).
- **V5 model + export target** (`v5_policy.py:29-152`; `contracts.py:27-36`; `v5_rlhf_adapter.py:104-125,271`): `V5ActionConditionedPolicy` (`policy_kind='v5_split_encoder'`) -- `base_encoder=Linear(1456,base_h)+SiLU`, `global_encoder=Linear(32,global_h)+SiLU`, `private_encoder=Linear(2400,private_h)+SiLU`, `history_encoder=Linear(3240,history_h)+SiLU`, `state_fuser=Linear(fused,256)+SiLU+Linear(256,256)+SiLU`, `action_encoder=Linear(171,128)`, `candidate_scorer=Linear(256+128,1)`, `value_head=Linear(256,1)`, `mana_draw_head=Linear(256,1)` (FRESH, no V4 counterpart). `__call__(obs, action_features, mana_draw_legal=None) -> (logits[601], value, mana_draw_logit)` 3-tuple. `OBS_V5_DIM=1456+32+2400+3240=7128`. The deploy adapter `_build_onnx_inference` (`v5_rlhf_adapter.py:118-121`) fixes the ONNX contract: inputs `observation[1,7128]` + `action_features[1,601,171]`, outputs `logits[1,601]` + `value[1,1]` + `mana_draw_logit[1,1]`. `_factory_v5_real` (`v5_rlhf_adapter.py:271`) is ALREADY registered as `reg.register('v5', _factory_v5_real)` (`policy_adapters.py:411`) -- the V5 factory slot is taken; E1 must NOT re-register the factory, only add a V5 kind DETECTOR (`policy_adapters.py:289 register_detector`, LIFO priority over `_sidecar_kind_detector`).
- **V4 export path (the mirror template, READ-ONLY)** (`export_onnx.py:16-190`; `model_mlx.py:37-114`; `onnx_policy.py:16-108`): `TorchActionConditionedPolicy` (`:19-65`) is V4-shaped (single `state_encoder` 1456->256->256, 2 outputs). `_WEIGHT_MAP` (`:68-74`) maps 5 V4 param groups. `load_torch_from_mlx_checkpoint` (`:77-124`) reads `.npz` (with `__meta__` JSON), builds the torch mirror, copies weights via `_WEIGHT_MAP` with a `.T` transpose fallback (`:118`). `export_checkpoint_to_onnx` (`:139-190`) calls `torch.onnx.export` with `dummy_obs=(1,1456)`, `dummy_af=(1,601,171)`, `input_names=['observation','action_features']`, `output_names=['logits','value']`, `dynamic_axes` batch, writes the sidecar `{model_version, source_checkpoint, obs_dim:1456, action_feature_dim:171, max_candidate_actions:601, placement_mode, inputs, outputs, config}`. The V5 exporter REUSES this TorchMirror->ONNX pattern + sidecar JSON shape but the torch module + weight map + dummy shapes + output names are NEW. `model_mlx.save_checkpoint` (`:89-114`) / `load_checkpoint` (`:117-162`) are generic `tree_flatten/unflatten` -- already round-trip V5 checkpoints (V5 param names like `base_encoder.layers.0.weight`, `mana_draw_head.weight`); `offline_replay_bridge.py:664-669` proves V5 load. E1 export reads the post-D V5 npz directly (no optimizer state needed -- `export_onnx.py:83-124` already skips `_opt_` keys via the weight map).
- **Prod bot brain + config (the ship target, LIVE prod files)** (`bot_brain.py:134-185,219-225,407-521,540-553`; `infrastructure/config.py:53-98,100-210,237-263`; `web/server.py:5744-5745,5298`): `BerserkInference._validate_train_v2_contract` REJECTS `obs_dim!=1456` (`:145`) + a non-`(logits,value)` output; `format != _TRAIN_V2_FORMAT='train_v2_classic_v1'` (`:37-40`) -> profile SKIPPED (`:219-225`). `_get_action_train_v2_classic` (`:407-521`) reads `logits[0]` (2-tuple assumption `:462`) + argmax over masked 601 -- NO mana_draw handling. `BOT_MODEL_PROFILES` (`config.py:53`) is the marker-keyed dict (`extra-lr-v4-lite/micro/opti/max`, each `format:'train_v2_classic_v1'`, `obs_dim:1456`). `BOT_STRENGTH_TIERS` (`:100-210`) maps trophy ranges -> `brain_profile` (a `BOT_MODEL_PROFILES` key) + selection softmax + level_policy; the TOP tiers (`tier_hard_4500` `:194`, `tier_hard_plus_6000` `:205`, `tier_max_minus_7500` `:216`, and `:227`) route `brain_profile:'extra-lr-v4-max'`. `BOT_DIFFICULTY_PROFILES` (`:243-263`) is DERIVED from `BOT_MODEL_PROFILES` keyed by tier `brain_profile` + aliases -- this is what `web/server.py:5745` loads into `BERSERK_BRAIN`. E1 ship = add `extra-lr-v5-max` to `BOT_MODEL_PROFILES` + retarget the top trophy tiers' `brain_profile` to `extra-lr-v5-max` (V4-max stays as the asymmetric H2H reference / lower-tier brain, SPEC `:71` roles 2/3). Any prod edit MUST be additive (new format branch / new profile key) and MUST NOT change V4 behavior or the trophy->brain_profile mapping for non-top tiers.
- **Acceptance gauntlet + gate logic (REUSE-AS-IS + ADAPT)** (`a_gate.py:88-144,164-224,309-434,484-678,711-886`; `gauntlet_v5.py:8-55`; `second_start_parity.py:58,113-266`; `block_b_gate.py:99-292,338-428`; `block_b_league_driver.py:150-194,427-547`; `opponents_v5.py:22-179`; `rust_live_self_play.py:131-335`; `run_v5_acceptance.py:14-95,288-380`; `run_v5_vs_v4max_benchmark.py`; `league_v5.py:125-199`): REUSE -- `NO_ASSIST_MIN_SCORE_RATE=0.55` (`a_gate.py:93`), `EXPLOIT_RESISTANCE_MIN_SCORE_RATE=0.50` (`:98`), `MANA_DRAW_BAND_LOW/HIGH=0.5/1.5` (`:103-104` + `check_mana_draw_band:309` with the `HAND_CAP/MANA_DRAW_BASE` invalidation guard), `H2H_PROMOTION_THRESHOLD=0.5` (`:113`), `check_h2h_trending` (`:374-434`, non-decreasing over `min_snapshots=5`, NO absolute floor), `select_promotion` (`:536-678`, external-bench ONLY + promotion-by-loss guard -- internal PPO loss/KL/entropy NEVER consulted), `play_gauntlet` + `GameRunner` Protocol (`:711-753`), `compute_score_rate=(wins+0.5*draws)/total` (`:131`). `PHASE_A_P1_P2_GAP_THRESHOLD=0.12` (`ppo_phaseA_config.py:120`) single-sourced via `second_start_parity.BLOCK_B_GAP_THRESHOLD` (`:58`). The retargeting to best self-snapshot is ALREADY DONE: `_DEFAULT_H2H_OPPONENT_KIND='best_ever'` (`block_b_league_driver.py:167`), `SelfPrevOpponent` (`rust_live_self_play.py:311` = frozen V5 snapshot), `V4MaxOpponent` (`:329` = V4-orig ONNX argmax absolute anchor), `SnapshotPool.best_ever` (`snapshot_pool.py`), B6 `_check_h2h_vs_best` (`block_b_gate.py:209`). `min_e2e_tps>=12000 / min_entropy>=0.70 / max_abs_kl<=0.12` EXIST as `run_v5_acceptance.py:33,35,37` CLI defaults; `min_no_bonus_p1/p2/second=0.75/0.70/0.70` (`:41,43,45`) EXIST but are measured vs V4-max (pre-baked JSON `:312-322`), while SPEC `:149` says vs best self-snapshot. ADAPT (E1 ADDS): `H2H_V4_ORIG_MIN=0.70` (NO constant exists; `acceptance_gate.py:15-16` has the TrainV2 `min_winrate_random=0.35`/`min_winrate_end_turn=0.80` -- wrong values, wrong surface); H2H vs Random ~1.0 + vs end_turn ~1.0 sanity gates (NO V5 harness plays these for H2H today); H2H-vs-best-self-snapshot ABSOLUTE floor ~0.52-0.55 (`check_h2h_trending` has trending but NO absolute floor, only the `H2H_PROMOTION_THRESHOLD=0.5` beat-current-best test); the no_bonus retarget (vs best self-snapshot, SPEC `:149`); the human-QA subjective panel (ZERO infrastructure). `run_v5_acceptance.py:16` broken `TrainV3` sys.path; `run_v5_vs_v4max_benchmark.py:26` has the SAME broken path. `league_v5.compare_adaptive_strength_monotonicity` (`:146`) is a SYNTHETIC proxy, NOT real H2H -- E1 must NOT present it as the H2H-vs-best-self-snapshot gate (`a_gate.py:33-36` flags this).
- **ONNX export-ship + parity + warm-start** (`release_bundle.py:60-64,106-201,291-402`; `tests/test_train_v2_onnx_export.py:82-85,143-176,209-291`; `tests/test_train_v2_warm_start_v5.py:122-158,197-239,241-268`; `warm_start_v5.py:39-91,176-277`; `mana_draw_head_v5.py:76-133`; `policy_adapters.py:155-187,219-248,269-305,404-416`; `policy_factory.py:379-396`): `release_bundle.discover_release_inputs` (`:106`) + `build_release_bundle` (`:291`) are FORMAT-AGNOSTIC (first `*.onnx` + `.onnx.json` sidecar + candidate.json + profile + overlay + shadow + acceptance_gate; hard-missing only `candidate_json/onnx` `:293`; writes `release_manifest.json` version `train_v2_release_bundle_v1`). REUSE AS-IS for V5 -- only the V5 onnx+sidecar must land in the candidate dir. `LOGIT_TOL=1e-3` is a FUNCTION-LOCAL at `tests/test_train_v2_onnx_export.py:237` (NOT a module constant); the pure-numeric ONNX<->MLX `allclose` ALREADY uses `atol=1e-4` (`:172-173`) and the MLX->Torch mirror uses `atol=1e-4` (`:82-85`) -- so `1e-4` is the de-facto numeric bar for V4. The `:224-236` comment pins Metal-vs-CPU drift at ~1e-8 for the V4 net (1456->256->256 + 171->128 + 384->1 + 256->1); V5 has a LARGER fused trunk (base/global/private/history encoders + state_fuser + mana_draw_head) -- drift MAY be larger. Warm-start Q3 PARTIAL relaxation is BINDING: `test_faithful_layer_exact_equality` (`:122-158`) asserts `base_encoder.layers.0` + `action_encoder` byte-match V4 (`atol=1e-7`); `test_full_logits_differ_beyond_tolerance` (`:197-239`) asserts the 601 logits INTENTIONALLY diverge >0.05 (`NOT allclose(atol=1e-3)`); `test_base_encoder_path_is_faithful_despite_logit_divergence` (`:241-268`) asserts the base-1456 path IS bit-identical. E1 must honor the relaxation (NOT re-assert 601-bit-close-to-V4-Max). The V4 ONNX fallback guard is `policy_adapters._factory_berserk:175-182` (RuntimeError on `has_profile False`); the V5 `_factory_v5_real` raises on missing onnxruntime (`v5_rlhf_adapter.py:108`) / missing path (`:98`) / import failure (`:144`) but has NO `has_profile`-style "silent rule-based fallback" check (V5 has no BerserkInference/brain layer) -- E1 must verify a V5 ONNX that loads but produces NaN/garbage logits cannot silently degrade (a logits-finite / argmax-legal guard at `V5RlhfAdapter.select_action` time, and the prod `_get_action_v5` path). `mana_draw_legal_mask` + `select_includes_mana_draw` (`mana_draw_head_v5.py:76,116`) EXIST but are UNWIRED in `onnx_policy.py` / `bot_brain.py` -- E1 must wire them into the prod V5 path. `_sidecar_kind_detector` (`policy_adapters.py:219-248`) routes `model_version=='classic_action_conditioned_onnx_v1'` -> `'action_onnx'`; E1 registers a NEW V5 detector via `register_detector` (LIFO, `:289`) keyed off the V5 `model_version` / `obs_dim==7128` / `mana_draw_head` flag -> kind `'v5'`.

---

## 2. User decisions (genuine -- present via AskUserQuestion AFTER this plan is verified)

Design defaults I can set are listed per-component in section 3. Recommendations are spec-literal unless flagged.

| ID | Question | Default | Options |
|----|----------|---------|---------|
| **E-E1** | **THE V5 exporter location (load-bearing).** The V4 `ai/train_v2/export_onnx.py` hardcodes `obs_dim=1456` + a single `state_encoder` + 2-tuple output + `MODEL_VERSION_ONNX='classic_action_conditioned_onnx_v1'` + a 5-group `_WEIGHT_MAP` (`:16,68-74,104,150`). V5 is `obs_dim=7128` + split-encoder + parallel `mana_draw_head` + 3-tuple (`v5_policy.py:65-152`). `export_onnx.py` is V4-frozen (pristine legacy, SPEC gamma) and a LIVE tracked file. Build the V5 exporter as a NEW file or extend `export_onnx.py` with a V5 branch? | **NEW file `TrainV3.5/python/train_v3/export_onnx_v5.py`** -- the V5 split-encoder + 3-tuple is structurally different; a new file keeps the V4 export path byte-untouched (SPEC gamma) and keeps V5 code in the untracked TrainV3.5 tree (the B/C/D pattern). | **NEW file** / extend `export_onnx.py` with a V5 branch (touches V4-frozen live file) |
| **E-E2** | **THE prod deploy path (load-bearing, the LARGEST E1 design choice).** `BerserkInference` (`ai/bot_brain.py`) is the single prod bot brain (`web/server.py:5745`); `_validate_train_v2_contract` (`:134-185`) REJECTS `obs_dim!=1456` (`:145`; it checks `logits` present at `:158-159` but does NOT assert a 2-tuple / reject a 3-tuple output), and `format != 'train_v2_classic_v1'` SKIPS the profile (`:219-225`). V5 (format `'v5'`, obs 7128) CANNOT load through it unchanged. How does V5 reach the prod arena? | **(a) Extend `BerserkInference` with a V5 format branch + `_get_action_v5`** -- a new `format='v5'` branch in `_validate_*` (accept `obs_dim=7128` + 3-tuple `outputs=['logits','value','mana_draw_logit']`) + a `_get_action_v5` path that reads the 3rd output, computes `mana_draw_legal_mask` (`mana_draw_head_v5.py:76`), applies `select_includes_mana_draw` (`:116`), and argmax-decodes the 601-best. Additive, V4 path unchanged. `ai/bot_brain.py` is a LIVE prod file -- the edit is additive (new branch, new profile key) and must not change V4 behavior. | **(a) Extend BerserkInference (V5 branch + _get_action_v5)** / (b) port `V5RlhfAdapter` into `web/server.py` as a separate V5 brain (new code in web/, reuses `encode_observation_v5` from TrainV3.5 -- but prod must NOT import TrainV3.5, so the encoder must be vendored) / (c) make `web/server.py` use the rlhf_env `policy_adapters` registry (cross-module prod->rlhf_env dependency) |
| **E-E3** | **THE tightened LOGIT_TOL value (load-bearing, SPEC :175/:189).** The V4 `LOGIT_TOL=1e-3` (`tests/test_train_v2_onnx_export.py:237`, function-local) is too loose -- a real V5 export bug <1e-3 slips. The existing V4 pure-numeric `allclose` ALREADY uses `atol=1e-4` (`:172-173,82-85`). V5 has a LARGER fused trunk; Metal-vs-CPU drift MAY be larger than V4's ~1e-8. | **1e-4, measure-then-pin** -- first MEASURE V5 Metal-vs-CPU drift on a real V5 checkpoint (a tiny export-drift probe), then pin `LOGIT_TOL_V5=1e-4` if drift stays near ~1e-8 (~10000x above the ~1e-8 float-noise floor, ~1000x below a real export bug O(0.1)); if drift is >1e-5, fall back to 5e-4 (compromise). Consistent with the existing V4 numeric bar. | **1e-4 (measure-then-pin)** / 1e-5 (aggressive, risks flake on the larger V5 trunk) / 1e-6 (likely flaky) / 5e-4 (compromise, pin directly) |
| **E-E4** | **H2H vs V4-orig >=0.70 lane (SPEC :145, the absolute anchor).** `run_v5_acceptance.py:312-322` reads PRE-BAKED winrates from a `--v4max-no-bonus-benchmark` JSON; SPEC :135 says "Rust ArenaEnv gauntlet" (implies LIVE games); SPEC :140 calls V4-orig the absolute anchor. | **LIVE Rust H2H via `V4MaxOpponent` + the pre-baked no-bonus corridor as a SECONDARY gate.** Wire `V4MaxOpponent` (`rust_live_self_play.py:329`) as a gauntlet lane through the A4 runner (new code, consistent with :135) for the H2H-vs-V4-orig score rate; KEEP the pre-baked `--v4max-no-bonus-benchmark` JSON path as a secondary no-bonus corridor check (it carries the p1/p2/second split). | **LIVE + pre-baked secondary** / LIVE only / pre-baked only (faster, diverges from :135) |
| **E-E5** | **H2H-vs-best-self-snapshot ABSOLUTE floor (SPEC :148).** `a_gate.check_h2h_trending` (`:374-434`) checks non-decreasing trending but has NO 0.52-0.55 absolute floor (only `H2H_PROMOTION_THRESHOLD=0.5` at `:113` for the beat-current-best test). SPEC :148 says "monotone improvement, >= ~0.52-0.55 over prior best." | **Hard gate: trending AND latest >= 0.52** -- add a new `H2H_VS_SELF_SNAPSHOT_MIN_FLOOR=0.52` constant in the E1 harness; the progression gate passes iff `check_h2h_trending` passes AND the latest H2H score rate >= 0.52 (the low end of the spec band -- conservative; 0.55 is the calibrate-up target). | **Hard gate (trending AND latest >= 0.52)** / calibration target only (record, do not fail) / hard gate at 0.55 (high end) |
| **E-E6** | **no_bonus p1/p2/second retarget (SPEC :149 "vs best self-snapshot").** `run_v5_acceptance.py:41-45` + `run_v5_vs_v4max_benchmark.py` measure no_bonus vs V4-max (p1>=0.75/p2>=0.70/second>=0.70 already there); SPEC :149 says vs best self-snapshot, `>= 0.70 each` (no p1=0.75 -- that 0.75 is a non-spec over-bench inherited from the V4-max script). | **Retarget the no_bonus gate to best self-snapshot (SPEC-literal) + KEEP V4-max no-bonus as a SECONDARY advisory.** The hard no_bonus gate is `second_start_parity.play_side_stratified_gauntlet` vs the best-self-snapshot opponent with the no-bonus corridor, `p1/p2/second >= 0.70 each` (SPEC :149, the spec scopes this row to self-snapshot). The V4-max pre-baked no-bonus read (`:41,43,45` p1>=0.75/p2>=0.70/second>=0.70) becomes a secondary asymmetric-anchor advisory (warn, not fail) -- note p1=0.75 there is STRICTER than spec :149's 0.70-each (a deliberate over-bench on the secondary read, NOT the spec value); it must NOT be the hard gate. | **Retarget no_bonus to best self-snapshot (SPEC :149, >= 0.70 each) + V4-max secondary advisory** / keep V4-max no-bonus as the gate + self-snapshot sanity (NON-spec default: p1=0.75 stricter than :149, demotes the spec row to advisory) / V4-max no-bonus only (drop the self-snapshot read) |
| **E-E7** | **H2H vs Random ~1.0 / vs end_turn ~1.0 (SPEC :146-147, "sanity").** `acceptance_gate.py:15-16` has the TrainV2 `min_winrate_random=0.35`/`min_winrate_end_turn=0.80` (NOT 1.0, NOT V5). No V5 gauntlet plays Random/end_turn for H2H today. Hard gates or sanity advisories? NOTE: SPEC :146-147 reads "~1.0 (sanity)" -- an approximate sanity advisory, NOT a hard numeric floor; the 0.95 hard-gate default below is a DELIBERATE tightening above the spec's ~1.0 intent (a candidate at 0.94 vs Random fails E1 despite the spec only requiring ~1.0 sanity). | **Hard gates >= 0.95** via `a_gate.play_gauntlet` vs `random`/`end_turn` opponent_kinds (the `opponents_v5.PHASE9_BROAD_OPPONENT_LANES` lists both). A candidate that drops a game to Random/end_turn has a real regression; ~1.0 is the spec's "100% vs Random" intent. | **Hard gates >= 0.95 (deliberate tightening above spec ~1.0 sanity)** / advisory (warn, not fail, spec-literal) / hard >= 0.99 (strict) |
| **E-E8** | **THE human-QA panel (SPEC :156, "subjective bar").** ZERO infrastructure exists (no subjective-difficulty scorer). The C2 deploy surface (`c2_collection_driver.C2CollectionDriver` + `V5RlhfAdapter`) puts a candidate vs humans in rlhf_env, but C2's stop condition is a mana_draw-floor/battle-cap (training-data objective), NOT subjective difficulty. | **Hybrid: in-worktree `E1HumanQAPanelDriver` (defines the verdict schema + stop condition) + USER-run operational panel.** The component reuses the C2 deploy surface + adds a subjective-difficulty capture (a post-battle reviewer scorecard JSON consumed by the E1 gate as a SOFT pass/fail -- a "subjectively harder" verdict is a release-checklist item with a programmatic soft-gate, NOT a hard numeric gate). The USER runs the panel (deploys each candidate vs humans, collects the scorecards). | **Hybrid (component + USER-run panel, soft gate)** / in-worktree component only (programmatic panel) / purely operational (manual sign-off, no component) |
| **E-E9** | **Final-acceptance gate surface (SPEC :136 names `run_v5_acceptance`).** `run_v5_acceptance.py` is read-only-per-A5-pattern + has the broken `TrainV3` path (`:16`) + reads pre-baked winrates + calls the synthetic `compare_adaptive_strength_monotonicity` proxy. Build a NEW E1 sibling or adapt `run_v5_acceptance.py` in place? | **NEW sibling `e1_tournament.py` (B6/D2 pattern).** Composes `a_gate` + `block_b_gate` + `snapshot_pool` + `second_start_parity` + `opponents_v5` + the A4 runner; adds the V4-orig/Random/end_turn/throughput/entropy/KL final lanes + the two-notion separation (absolute vs progression). `run_v5_acceptance.py` stays READ-ONLY (its threshold CLI defaults are reused as constants where they match the spec). | **NEW sibling `e1_tournament.py`** / adapt `run_v5_acceptance.py` in place (fix path + wire live games + raise thresholds) |
| **E-E10** | **Ship artifact format + mana_draw head (SPEC :158, :186).** Ship an ONNX 3-output (head exported + wired) or an ONNX 601-only (head stripped) or an MLX npz? SPEC :158 says "export best -> ONNX + sidecar; deploy to prod arena (argmax)"; SPEC :186 says "selection combines 601-candidate-best vs mana_draw logit correctly." | **ONNX 3-output (head exported + wired).** Export `mana_draw_logit` as the 3rd ONNX output; wire `mana_draw_legal_mask` + `select_includes_mana_draw` into the prod `_get_action_v5` path (E5). Full V5 behavior. Stripping the head regresses V5 vs V4-orig on the mana_draw axis; MLX npz avoids ONNX but prod has no MLX runtime (`bot_brain` uses onnxruntime). | **ONNX 3-output (head exported + wired)** / ONNX 601-only (strip head, prod never takes mana_draw) / MLX npz + new MLX prod runtime |
| **E-E11** | **Ship marker scope (SPEC :158).** Ship ONLY `extra-lr-v5-max` (top-tier replacement) or also a v5 temperature-variant family (v5-lite/opti) for lower tiers? SPEC :158 mentions "snapshots/temperature variants feed the next iteration's league pool" -- suggests max-only ship now, variants later. | **Max-only now.** Ship `extra-lr-v5-max` as the top-tier brain (retarget `tier_hard_4500`/`tier_hard_plus_6000`/`tier_max_minus_7500` + the `:227` tier); leave `extra-lr-v4-max` as the asymmetric H2H reference / lower-tier brain (SPEC :71 roles 2/3). Variants are a post-E1 league-pool concern. | **Max-only now (variants later)** / max + v5-lite/opti family now |
| **E-E12** | **E1 runner (the prior-block pattern).** The D-league RUN was USER-run operational glue (no `block_d_runner.py` in-worktree); the C-loop RUN was USER-run per D-C0/D-C9. Should E1 follow the same pattern or include a thin in-worktree runner? | **Hybrid: thin in-worktree `block_e1_runner.py` wrapper for repeatability + USER-run operational RUN.** The components (E1-E5) are synthetic-tested; the runner is a thin CLI that wires `E1CandidateSet` -> `e1_tournament` -> `e1_human_qa_panel` -> `export_onnx_v5` -> `e1_ship` and is the operational entry the USER invokes (gated on the D-league RUN producing a `BlockDLeagueManifest.candidate_paths`). Mirrors the D-D6 "thin runner is an E1-prep operational artifact" note. | **Hybrid (thin runner + USER-run)** / USER-run only (no runner component) / full in-worktree runner |
| **E-E13** | **Sidecar version string + V5 kind-detector key (SPEC :158 "_derive_kind_from_sidecar").** The V4 `_sidecar_kind_detector` (`policy_adapters.py:219-248`) returns `'action_onnx'` if ANY of three OR-branches fires: (a) `model_version=='classic_action_conditioned_onnx_v1'` (`:239`), (b) `{'observation','action_features'}.issubset(inputs)` (`:240`), (c) `sidecar.get('action_feature_dim') is not None` (`:241`). The V5 sidecar emits `inputs=['observation','action_features']` AND `action_feature_dim:171`, so it MATCHES the V4 detector via branches (b) AND (c) regardless of `model_version` -- a distinct `model_version` prevents only branch (a), NOT the full-clause match. V5 routing therefore relies on the LIFO V5 detector (`register_detector` insert-at-0, `:289`) returning `'v5'` BEFORE the V4 detector runs -- a LOAD-BEARING invariant (if the V5 detector is missing/returns None, the V4 detector misclassifies V5 as `action_onnx`). What `model_version` does the V5 sidecar emit, and what does the V5 detector key off? | **`model_version='v5_split_encoder_onnx_v1'` + `'mana_draw_head': true` + `'format':'v5'` + `obs_dim:7128`.** The V5 detector (registered via `register_detector`, LIFO over `_sidecar_kind_detector`) keys off the V5 `model_version` string (and/or `obs_dim==7128` + `mana_draw_head:true`) and MUST return kind `'v5'` (never None) for any V5 sidecar so the LIFO ordering is load-bearing. The `format:'v5'` field lets `BerserkInference` (`bot_brain.py:219`) SKIP-or-route it cleanly if it ever sees it. A regression test asserts the V5 detector returns `'v5'` (not None) for a V5 sidecar (LIFO load-bearing-tested). | **`v5_split_encoder_onnx_v1` + mana_draw_head:true + format:v5** / user pins a different string |
| **E-E14** | **Warm-start parity ship-gate scope (SPEC :176, Q3 PARTIAL).** SPEC :176 literally says "V5 601-scorer + base-1456 path forward-pass bit-close to V4-Max immediately after load." Block 0 RELAXED this to faithful-layer equality (`test_faithful_layer_exact_equality:122-158`, `atol=1e-7` on `base_encoder.layers.0` + `action_encoder`) + base-1456 path identity (`test_base_encoder_path_is_faithful:241-268`); the 601 logits INTENTIONALLY diverge >0.05 (`:197-239`). NOTE: `test_base_encoder_path_is_faithful:263-268` asserts a LAYER-output identity (`base_encoder.layers.0` output == V4 `state_encoder.layers.0` output), NOT a forward-pass-to-logits identity -- a base-1456-only forward pass to the 601 logits still diverges because V5 `state_fuser.layers.0` is a FRESH `Linear(fused,256)` with a different input dim than V4 `state_encoder.layers.0` (`Linear(1456,256)`), so even with the extra dims zeroed the fused trunk differs (`:197-239` documents >0.05 divergence). | **Re-assert the existing faithful-layer gate + base-1456 path identity (Q3 PARTIAL consistent); the bonus is the faithful LAYER-output bit-close on a frozen base-1456 input (already covered by `test_base_encoder_path_is_faithful:241-268`), NOT a forward-pass-to-logits bit-close.** Do NOT add a full-forward logits bit-close on base-1456-only input (architecturally impossible -- fresh `state_fuser.layers.0` + extra SiLU); the bonus is redundant with the existing layer-output identity test, kept as an explicit re-assert on the shipped artifact. Do NOT re-assert 601-bit-close. | **Faithful-layer + base-1456 path identity (bonus = faithful LAYER-output bit-close, NOT forward-to-logits)** / faithful-layer + base-1456 path identity only (strict Q3 PARTIAL, drop redundant bonus) / attempt full 601-bit-close (architecturally impossible) |

**Implementer defaults (surfaced for visibility, NOT user decisions):**
- **E-E15 -- Tightened-tolerance scope:** apply `LOGIT_TOL_V5` to ALL 3 V5 outputs (601 logits + value + mana_draw_logit), with a SEPARATE (possibly looser) `MANA_DRAW_LOGIT_TOL` for the mana_draw scalar (a fresh head, no V4 baseline, drift behavior may differ from the 601 head). The V4 test covers logits+value only; E1 adds the mana_draw axis. Implementer default under E-E3.
- **E-E16 -- Measure V5 Metal-vs-CPU drift before pinning:** the measure-then-pin probe (a tiny V5 export-drift test on a real V5 checkpoint) is the safe path; coupled to E-E3. If MLX is unavailable in the test env, skip-gate the probe and pin `1e-4` directly (the V4 numeric-bar-consistent value).
- **E-E17 -- Gauntlet roster:** UNIFIED roster `{V4-orig (absolute), Random, end_turn, best_self_snapshot (progression), 7 EXPLOIT_AGENT_KINDS}` with per-lane thresholds from SPEC :143-155 (the V4-orig/Random/end_turn/best-self-snapshot lanes are H2H score-rate gates; the 7 exploit lanes feed `check_exploit_resistance_gate` >= 0.50). Reuses `opponents_v5.PHASE9_BROAD_OPPONENT_LANES` + `gauntlet_v5.EXPLOIT_AGENT_KINDS`.
- **E-E18 -- Candidate load:** `model_mlx.load_checkpoint` (V5-native) for ALL 3 candidates (post-D/post-C3/post-B are all V5 npz saved via `model_mlx.save_checkpoint` with V5 param names; C3 spec note #2 flags V5-BC-seed vs V4-Max npz key-name mismatch, but the E1 candidates are post-B/C/D V5 checkpoints, NOT the V4-Max warm-start seed -- so V5-native load is correct). The V4-Max npz is READ-ONLY and only used by `warm_start_v5.load_v4_max_into_v5` at training start, NOT by E1.
- **E-E19 -- Short-D veto (confirm-only, `BLOCK_D_COMPLETION.md:107`):** confirm or veto the short-D schedule that produced post-D before E1 eval. Implementer default = confirm short-D as-is (eval all 3 candidates; if post-D is short, post-C3/post-B are still evaluated).
- **E-E20 -- Next-iteration seed manifest (SPEC :158 "feed the next iteration's league pool"):** OUT of E1 scope -- a separate post-E1 artifact (the E1 ship emits the marker + the bundle; the next-iteration league pool seeding is a follow-up). E1 records the candidate set + temperature variants in the ship README for traceability.
- **E-E21 -- Export source (confirm):** export from the post-D trained V5 npz directly via `model_mlx.load_checkpoint` (a standard V5 npz, no optimizer state needed -- `export_onnx.py:83-124` skips `_opt_` keys via the weight map). E1 asserts this in the plan.

**Frozen / non-decisions (do NOT re-litigate):**
- **gamma** -- frozen v1 codec (`classic_obs_v1`/`classic_actions_v1`/`classic_card_shape_v1` byte-frozen); V4-orig ONNX runs unchanged forever (pristine legacy, zero adapter); V5 reuses the 601/171 candidate block unchanged; `mana_draw` is a PARALLEL binary head, NOT a 602nd candidate.
- **D6/D10** -- V4-orig's 3 roles (warm-start source / asymmetric H2H reference / temperature-spectrum sparring lane); acceptance stays on Rust (`gauntlet_v5`/`run_v5_acceptance` retargeted to best self-snapshot, already done at Block B); V4-orig is the ABSOLUTE anchor (>=0.70), NOT the progression bar.
- **D11** -- omniscient always (V5-Max is server-side; the prod `_get_action_v5` path has the info edge over humans -- accepted given the "subjectively harder" goal).
- **Promotion-by-loss guard** -- the E1 tournament winner selection consults ONLY external-bench criteria (A-gate + H2H + the threshold table); internal PPO loss/KL/entropy are monitoring-only (`a_gate.select_promotion:607`, SPEC :69/:112).
- **Q3 PARTIAL relaxation** -- warm-start full-forward-pass-parity is RELAXED to faithful-layer equality; the 601 logits INTENTIONALLY diverge >0.05. E1 honors this (E-E14).
- **rlhf_env scope (D-C0 carry)** -- the worktree's `rlhf_env/components/*.py` IS git-tracked (recoverable via `git checkout`); the C1 V5 adapter factory `_factory_v5_real` is already registered. E1 adds only a V5 kind DETECTOR + (per E-E2) the prod `_get_action_v5` path in `ai/bot_brain.py`.

> **Why E-E2 is THE user decision (do not resolve as an implementer choice):** the prod deploy path is the single largest E1 design choice. Option (a) edits `ai/bot_brain.py` -- a LIVE prod file loaded by `web/server.py:5745` for every live bot turn -- but the edit is additive (a new `format='v5'` branch + a new `_get_action_v5` path + a new `BOT_MODEL_PROFILES` key) and keeps V4 behavior byte-unchanged. Option (b) ports `V5RlhfAdapter` into `web/server.py` but prod must NOT import TrainV3.5 (`BLOCK_C_COMPLETION.md:81`), so `encode_observation_v5` would need to be vendored into prod -- a large surface. Option (c) makes prod depend on the rlhf_env `policy_adapters` registry -- a cross-module prod->rlhf_env dependency. The spec (:158 "deploy to prod arena argmax") + the existing `BerserkInference` single-load-point architecture favor (a); the prod-safety risk of editing a live file favors (b)/(c). This is genuinely the user's call.

> **DECISIONS-CONFIRMED (2026-07-02, AskUserQuestion):** the 4 load-bearing genuine-user decisions confirmed = the recommended defaults:
> - **E-E2 = Extend BerserkInference** (additive `format='v5'` branch + `_get_action_v5` in `ai/bot_brain.py`; V4 path byte-unchanged).
> - **E-E3 = 1e-4, measure-then-pin** (measure V5 Metal-vs-CPU drift first, pin `LOGIT_TOL_V5=1e-4` if drift near ~1e-8, fall back to 5e-4 if drift >1e-5).
> - **E-E8 = Hybrid (in-worktree `E1HumanQAPanelDriver` + USER-run operational panel, soft gate)** (reuses the C2 deploy surface + a reviewer scorecard JSON consumed as a SOFT pass/fail).
> - **E-E12 = Hybrid (thin in-worktree `block_e1_runner.py` + USER-run)** (CLI wires E1CandidateSet -> e1_tournament -> e1_human_qa_panel -> export_onnx_v5 -> e1_ship; gated on the D-league RUN producing `candidate_paths`).
>
> The remaining decisions execute under the spec-literal / safety-conservative defaults (the plan's recommendations, already merged by the verify-only Finalize): **E-E1** = NEW file `export_onnx_v5.py` (V4 export path byte-untouched); **E-E4** = LIVE Rust H2H via `V4MaxOpponent` + pre-baked no-bonus corridor as secondary; **E-E5** = hard gate (trending AND latest >= 0.52); **E-E6** = retarget no_bonus to best self-snapshot >= 0.70 each + V4-max no-bonus secondary advisory (spec-literal, :149); **E-E7** = hard gates >= 0.95 (deliberate tightening above spec ~1.0 sanity); **E-E9** = NEW sibling `e1_tournament.py` (run_v5_acceptance.py stays READ-ONLY); **E-E10** = ONNX 3-output (mana_draw head exported + wired); **E-E11** = max-only now (extra-lr-v5-max, variants later); **E-E13** = `model_version='v5_split_encoder_onnx_v1'` + `mana_draw_head:true` + `format:'v5'` + obs_dim 7128, V5 detector registered LIFO (load-bearing invariant); **E-E14** = faithful-layer + base-1456 path identity + faithful LAYER-output bit-close bonus (NOT forward-to-logits; Q3 PARTIAL binding). Execution proceeds with these frozen.

---

## 3. Components (dependency-ordered, synthetic-testable)

> **Component -> spec-phase mapping (do not conflate):** plan components E1-E5 (the in-worktree components) are DISTINCT from the pipeline stage "Block E1" (the whole tournament+ship stage). A reader can conflate plan-E1 (the V5 exporter) with stage-Block-E1; they are distinct. Mapping: **E1** = the V5 ONNX exporter (E-E1, E-E10, E-E13, the V5 torch mirror + 3-tuple + sidecar) -- **E2** = the V5 export-parity + integrity test (E-E3, E-E14, the tightened LOGIT_TOL + mana_draw parity + warm-start re-assert + fallback guard) -- **E3** = the tournament harness + final-acceptance gate (E-E4..E-E9, E-E17, the Rust gauntlet over the 3 candidates + the SPEC :143-156 threshold table + the two-notion winner selection) -- **E4** = the human-QA panel driver (E-E8, C2 deploy surface reuse + subjective verdict) -- **E5** = the ship component (E-E2, E-E11, E-E13, the V5 kind detector + release_bundle composition + prod deploy wiring + ONNX fallback guard verify). E3 (eval) and E1/E2 (export) are INDEPENDENT tracks; E4 depends on E3 (needs the winner); E5 depends on E1, E2, E3, E4.

### E1. `export_onnx_v5.py` -- the V5 ONNX exporter (E-E1, E-E10, E-E13, load-bearing)
- **File:** NEW `TrainV3.5/python/train_v3/export_onnx_v5.py` (E-E1 = new file; the V4 `ai/train_v2/export_onnx.py` is V4-frozen + LIVE tracked, NOT touched).
- **`TorchV5ActionConditionedPolicy(torch.nn.Module)`** -- a torch mirror of `v5_policy.V5ActionConditionedPolicy` (`:65-140`): `base_encoder=Sequential(Linear(1456,base_h),SiLU)`, `global_encoder=Sequential(Linear(32,global_h),SiLU)`, `private_encoder=Sequential(Linear(2400,private_h),SiLU)`, `history_encoder=Sequential(Linear(3240,history_h),SiLU)`, `state_fuser=Sequential(Linear(fused,256),SiLU,Linear(256,256),SiLU)`, `action_encoder=Linear(171,128)`, `candidate_scorer=Linear(256+128,1)`, `value_head=Linear(256,1)`, `mana_draw_head=Linear(256,1)`. `forward(observation, action_features) -> (logits[B,601], value[B], mana_draw_logit[B])` 3-tuple (mirrors `v5_policy.__call__:106-152` but WITHOUT the `mana_draw_legal` mask arg -- the exported ONNX emits the RAW mana_draw_logit; the legal mask is applied at inference time by the prod `_get_action_v5` path / `V5RlhfAdapter.select_action`). The `encode_state` split (base `[:, :1456]`, global `[:, 1456:1488]`, private `[:, 1488:3888]`, history `[:, 3888:7128]`) mirrors `v5_policy.encode_state:87-104`.
- **`V5_WEIGHT_MAP`** -- the V5 param-level map (REUSES the `export_onnx.py:68-74` pattern, NEW names): `base_encoder.layers.0`, `global_encoder.layers.0`, `private_encoder.layers.0`, `history_encoder.layers.0`, `state_fuser.layers.0`, `state_fuser.layers.2`, `action_encoder`, `candidate_scorer`, `value_head`, `mana_draw_head` (each `.weight` + `.bias`). Reads V5 npz keys (saved via `model_mlx.save_checkpoint` -> V5 nn.Module param names). Reuses the `load_torch_from_mlx_checkpoint` pattern (`export_onnx.py:77-124`) with the `.T` transpose fallback (`:118`).
- **`load_v5_torch_from_mlx_checkpoint(checkpoint_path, *, hidden_dim=None, action_hidden_dim=None, base_hidden_dim=None, private_hidden_dim=None, history_hidden_dim=None, global_hidden_dim=None) -> (model, metadata)`** -- reads the V5 `.npz` (`__meta__` JSON), builds `TorchV5ActionConditionedPolicy(obs_dim=7128, action_feature_dim=171, ...)`, copies weights via `V5_WEIGHT_MAP`. Asserts `obs_dim==7128` (rejects a V4 npz misrouted here).
- **`export_v5_checkpoint_to_onnx(checkpoint_path, output_path, *, opset=17, placement_mode=None) -> str`** -- `torch.onnx.export` with `dummy_obs=(1,7128)`, `dummy_af=(1,601,171)`, `input_names=['observation','action_features']`, `output_names=['logits','value','mana_draw_logit']`, `dynamic_axes` batch on all 4. Writes the V5 sidecar (REUSES the `:174-188` JSON shape, ADAPTED fields per E-E13): `{model_version:'v5_split_encoder_onnx_v1', source_checkpoint, obs_dim:7128, action_feature_dim:171, max_candidate_actions:601, placement_mode, inputs:['observation','action_features'], outputs:['logits','value','mana_draw_logit'], mana_draw_head:true, format:'v5', card_shape_version:'v5_card_shape_v1', config}`. The 601/171 candidate block stays frozen (SPEC gamma); `mana_draw` is the 3rd output, NOT a 602nd candidate.
- **CLI `_main`** -- `--checkpoint`, `--output`, `--opset`, `--placement-mode` (mirrors `export_onnx.py:193-209`).
- **Depends on:** `v5_policy.V5ActionConditionedPolicy` (READ-ONLY, the architecture to mirror), `contracts.OBS_V5_DIM/ACTION_FEATURE_DIM` (READ-ONLY), `model_mlx.save_checkpoint/load_checkpoint` (READ-ONLY, the npz format), `ai.train_v2.v5_card_shape_v1.CARD_SHAPE_VERSION` (READ-ONLY, the sidecar field). NO edit to `export_onnx.py` / `model_mlx.py` / `onnx_policy.py` (V4-frozen).
- **Constraints:** the 601 candidate logits output stays shape `[B,601]` (NO 602nd column); `mana_draw_logit` is a PARALLEL scalar head; the V5 sidecar `model_version` is distinct from the V4 string (E-E13) BUT a distinct `model_version` alone does NOT prevent the V4 `_sidecar_kind_detector` (`policy_adapters.py:219-248`) from matching the V5 sidecar via the `inputs/action_feature_dim` OR-branches (`:240-241`) -- V5 routing relies on the LIFO V5 detector (`register_detector`, `:289`) returning `'v5'` (never None); `obs_dim=7128` in the sidecar (NOT 1456).
- Tests: synthetic (skip-gated on `torch`/`onnx`/`onnxruntime` absent) -- build a V5 tiny checkpoint (`V5ActionConditionedPolicy` with small hidden dims, save via `model_mlx.save_checkpoint`), export via `export_v5_checkpoint_to_onnx`, assert (a) the ONNX has 3 outputs named `logits/value/mana_draw_logit`; (b) `logits` shape `[1,601]`, `value` `[1,1]`, `mana_draw_logit` `[1,1]`; (c) the sidecar `model_version=='v5_split_encoder_onnx_v1'` + `obs_dim==7128` + `mana_draw_head==true` + `format=='v5'`; (d) the FULL detector chain (V5 detector registered LIFO via `register_detector`) returns `'v5'` for the V5 sidecar AND the V4 `_sidecar_kind_detector` ALONE returns `'action_onnx'` for the V5 sidecar (documenting the hard LIFO dependency -- a missing/unregistered V5 detector is caught as a regression, since the V4 detector matches V5 via the `inputs/action_feature_dim` OR-branches `policy_adapters.py:240-241`); (e) `load_v5_torch_from_mlx_checkpoint` rejects a V4 npz (obs_dim mismatch); (f) onnxruntime session.run returns 3 outputs matching the MLX forward within the tightened tol (delegates the parity assertion to E2's dedicated test, but a smoke numeric check here).

### E2. `test_export_onnx_v5.py` -- V5 export-parity + integrity test (E-E3, E-E14, load-bearing)
- **File:** NEW `TrainV3.5/python/train_v3/test_export_onnx_v5.py` (or `tests/test_export_onnx_v5.py` -- colocated with E1; the V4 `tests/test_train_v2_onnx_export.py` is V4-frozen, NOT edited).
- **`LOGIT_TOL_V5` (module-level)** -- the tightened tolerance (E-E3 = 1e-4 measure-then-pin). A `MANA_DRAW_LOGIT_TOL_V5` (E-E15, default = `LOGIT_TOL_V5` or a looser 5e-4 if the measure-then-pin probe shows the fresh head drifts more). The V4 `LOGIT_TOL=1e-3` at `tests/test_train_v2_onnx_export.py:237` is NOT edited (frozen-classic).
- **`test_v5_onnxruntime_matches_mlx`** -- the V5 analogue of `tests/test_train_v2_onnx_export.py:143-176,209-291`: build a V5 tiny checkpoint, export via E1, drive MLX (Metal) + ONNX-runtime (CPU) over N env steps; assert (1) `max|logits_mlx - logits_onnx| < LOGIT_TOL_V5` (numeric fidelity); (2) `|value_mlx - value_onnx| < LOGIT_TOL_V5`; (3) `|mana_draw_logit_mlx - mana_draw_logit_onnx| < MANA_DRAW_LOGIT_TOL_V5` (the NEW 3rd-output axis, no V4 counterpart); (4) when the MLX top-2 601 margin > `LOGIT_TOL_V5`, argmax MLX == ONNX (behavioral); (5) the mana_draw selection (`select_includes_mana_draw(mana_draw_logit, best_candidate_logit, mana_draw_legal)`) agrees MLX vs ONNX when the margin > tol. SCOPE: assertions (3)+(5) validate the mana_draw head + the standalone `select_includes_mana_draw` selection function in ISOLATION; the rlhf_env deploy adapter `V5RlhfAdapter.select_action` (`v5_rlhf_adapter.py:201`) currently DISCARDS `_mana_draw_logit` (underscore-bound, never used -- the adapter argmaxes ONLY over the 601 logits, `mana_draw_legal_mask`/`select_includes_mana_draw` are NEVER referenced in `v5_rlhf_adapter.py`), so the rlhf_env deploy path is mana_draw-BLIND and its mana_draw behavior is NOT validated here. Only the prod `_get_action_v5` path (E5, which wires the helpers into `ai/bot_brain.py`) exercises the 3rd output in a deploy path. E2's parity covers the head + the standalone selection function; E4's human-QA panel (which uses the mana_draw-blind `V5RlhfAdapter`) must be interpreted accordingly -- a V5 bot in the panel never takes mana_draw actions.
- **`test_v5_metal_vs_cpu_drift_probe` (E-E16, measure-then-pin)** -- a probe that measures the actual V5 Metal-vs-CPU drift on a real V5 checkpoint (skip-gated if MLX/unbuildable); emits the measured drift so E-E3 can be pinned data-driven. If MLX is unavailable, skip-gate and pin `LOGIT_TOL_V5=1e-4` directly (the V4 numeric-bar-consistent value).
- **`test_v5_warm_start_faithful_layer_equality` (E-E14)** -- RE-ASSERTS the Q3 PARTIAL warm-start gate on the SHIPPED artifact: `np.testing.assert_array_equal` + `np.allclose(atol=1e-7)` on `base_encoder.layers.0` + `action_encoder` vs the V4-Max source (mirrors `tests/test_train_v2_warm_start_v5.py:122-158`); `test_v5_base_encoder_path_is_faithful` -- the base-1456 path identity (`:241-268`); `test_v5_base_1456_faithful_layer_bit_close` (E-E14 BONUS) -- a faithful LAYER-output bit-close: `base_encoder.layers.0` output on a frozen base-1456 input matches V4 `state_encoder.layers.0` output (the identity `test_base_encoder_path_is_faithful:263-268` already asserts; the bonus re-asserts it on the SHIPPED artifact). NOT a forward-pass-to-logits bit-close -- a base-1456-only forward to the 601 logits diverges because V5 `state_fuser.layers.0` is a fresh `Linear(fused,256)` with a different input dim than V4 `state_encoder.layers.0` (`:197-239` documents >0.05 divergence); `test_v5_601_logits_intentionally_diverge` -- asserts NOT `allclose(v5_logits, v4_logits, atol=1e-3)` AND max_diff > 0.05 (mirrors `:197-239`, honors Q3 PARTIAL -- do NOT assert 601-bit-close). The V4-Max npz is READ-ONLY (skip-gated if absent in the worktree).
- **`test_v5_onnx_fallback_guard` (SPEC :174)** -- verifies the V5 deploy path CANNOT silently degrade to a rule-based policy: (a) `V5RlhfAdapter` raises on missing onnxruntime / missing path / import failure (`v5_rlhf_adapter.py:98,108,144`); (b) a V5 ONNX that loads but produces NaN/garbage logits is caught -- a NEW logits-finite / argmax-legal guard FACTORED as a standalone pure function `_assert_v5_logits_finite_legal(logits, legal_mask)` (raises `RuntimeError` on NaN/no-legal-candidate; the current `V5RlhfAdapter.select_action:206-208` does a SILENT `return 0`, NOT a RuntimeError, so the guard does NOT exist yet). E2 tests the standalone function in ISOLATION with a fake `InferenceSession` returning NaN/garbage (E2-green does NOT depend on E5); E5 then WIRES the tested function into `V5RlhfAdapter.select_action` and the prod `_get_action_v5` path; (c) the prod `_get_action_v5` path raises on a malformed V5 ONNX (NOT a silent rule-based fallback). The V4 guard (`policy_adapters._factory_berserk:175-182` RuntimeError on `has_profile False`) stays unchanged.
- **`test_v5_sidecar_codec_sync_invariant` (SPEC :177, Block -1 carry made verifiable at ship time)** -- a one-line invariant: assert the shipped V5 sidecar's `card_shape_version` equals `ai.train_v2.v5_card_shape_v1.CARD_SHAPE_VERSION` AND that this matches the rlhf_env encoder's ruleset version (the codec-sync invariant -- traces are only consumable if rlhf_env encodes under the frozen ruleset, SPEC :177 delegates enforcement to Block -1; this assert closes the loop end-to-end at ship time rather than relying solely on the Block -1 carry).
- **Depends on:** E1 `export_v5_checkpoint_to_onnx` + `TorchV5ActionConditionedPolicy`, `v5_policy.V5ActionConditionedPolicy`, `mana_draw_head_v5.mana_draw_legal_mask/select_includes_mana_draw`, `warm_start_v5.load_v4_max_into_v5` (READ-ONLY), `v5_rlhf_adapter.V5RlhfAdapter` (for the fallback-guard test). NO edit to `tests/test_train_v2_onnx_export.py` / `tests/test_train_v2_warm_start_v5.py` (V4-frozen).
- **Constraints:** the tightened `LOGIT_TOL_V5` lives in the NEW V5 test (NOT editing the V4 `LOGIT_TOL=1e-3`); the warm-start assertions HONOR Q3 PARTIAL (no 601-bit-close); the V4-Max npz is READ-ONLY (skip-gated if gitignored/absent).
- Tests: the file IS the test (skip-gated on `torch`/`onnx`/`onnxruntime`/`mlx`/V4-Max-npz absent). Synthetic-where-possible (a fake InferenceSession for the fallback-guard test; the parity probe needs real MLX+ONNX).

### E3. `e1_tournament.py` -- tournament harness + final-acceptance gate (E-E4..E-E9, E-E17, load-bearing)
- **File:** NEW `TrainV3.5/python/train_v3/e1_tournament.py` (E-E9 = new sibling, B6/D2 pattern; `run_v5_acceptance.py` stays READ-ONLY).
- **Threshold constants (E-E5, E-E6, E-E7, E-E17):** `H2H_V4_ORIG_MIN=0.70` (NEW, E-E4 -- NO constant exists; the absolute anchor); `H2H_VS_RANDOM_MIN=0.95` + `H2H_VS_END_TURN_MIN=0.95` (NEW, E-E7 -- the sanity gates, spec ~1.0); `H2H_VS_SELF_SNAPSHOT_MIN_FLOOR=0.52` (NEW, E-E5 -- the progression absolute floor, low end of the spec band); REUSE `NO_ASSIST_MIN_SCORE_RATE=0.55` (`a_gate.py:93`), `EXPLOIT_RESISTANCE_MIN_SCORE_RATE=0.50` (`:98`), `MANA_DRAW_BAND_LOW/HIGH=0.5/1.5` (`:103-104`), `p1_p2_max_score_gap <= 0.12` (REUSE `PHASE_A_P1_P2_GAP_THRESHOLD=0.12`, `ppo_phaseA_config.py:120` via `second_start_parity.BLOCK_B_GAP_THRESHOLD:58`; <= direction mirrors SPEC :152, consistent with how `max_abs_kl<=0.12` is stated), `min_e2e_tps=12000/min_entropy=0.70/max_abs_kl=0.12` (`run_v5_acceptance.py:33,35,37`), `no_bonus p1/p2/second >= 0.70 each vs best self-snapshot` (SPEC :149, the HARD gate via `second_start_parity.play_side_stratified_gauntlet` + the no-bonus corridor; E-E6 = retarget to self-snapshot); the V4-max pre-baked `p1>=0.75/p2>=0.70/second>=0.70` (`:41,43,45`) is the SECONDARY advisory (p1=0.75 is STRICTER than spec :149's 0.70-each, a deliberate over-bench on the secondary read, NOT the spec value).
- **`E1TournamentConfig`** -- the candidate set (`E1CandidateSet`, READ-ONLY consumption), the gauntlet roster (E-E17 = unified `{V4-orig, Random, end_turn, best_self_snapshot, *EXPLOIT_AGENT_KINDS}`), `games_per_opponent`, `games_per_opponent_per_side` (side-stratified for p1/p2), seeds, the throughput/entropy/KL floors, the no_bonus benchmark JSON path (E-E6 secondary).
- **`E1CandidateReport`** -- per-candidate: the measured H2H vs each lane (V4-orig score rate, Random, end_turn, best-self-snapshot latest + the trending series), the no_assist/exploit_resistance score rates, the mana_draw rate + baseline, the p1_p2 gap, the no_bonus p1/p2/second (from the self-snapshot gauntlet [HARD gate, SPEC :149] + the pre-baked V4-max JSON [secondary advisory]), the throughput/entropy/KL, the human-QA verdict (from E4, soft), and the per-criterion `GateOutcome` (pass/warn/fail) + the aggregate verdict.
- **`run_e1_tournament(config, *, game_runner, candidate_loader) -> list[E1CandidateReport]`** -- for each candidate path in `E1CandidateSet` (Nones dropped, post-D first): load the candidate into a `V5ActionConditionedPolicy` via `candidate_loader` (E-E18 = `model_mlx.load_checkpoint`, V5-native); play the unified gauntlet via `a_gate.play_gauntlet(game_runner, opponent_kinds, games_per_opponent, seed)` (`a_gate.py:753`) -- the `game_runner` is the A4 `rust_live_self_play` production wiring (`rust_live_self_play.py:131-335`: `SelfPrevOpponent` for best-self-snapshot, `V4MaxOpponent` for V4-orig, the rule-agent dispatch for exploit/Random/end_turn); measure side-stratified via `second_start_parity.play_side_stratified_gauntlet` (`:113`) for the p1/p2 gap + the best-self-snapshot H2H; apply `a_gate.check_no_assist_gate` / `check_exploit_resistance_gate` / `check_mana_draw_band` / `check_h2h_trending` (`:164,194,309,374`) + the NEW `H2H_VS_SELF_SNAPSHOT_MIN_FLOOR` absolute-floor check (E-E5 = trending AND latest >= 0.52); apply the V4-orig/Random/end_turn H2H gates (E-E4, E-E7); apply the throughput/entropy/KL floors (read from the candidate's run artifacts, like `run_v5_acceptance._candidate_gates`); apply the no_bonus gates (E-E6 = self-snapshot hard gate >= 0.70 each + V4-max pre-baked secondary advisory). Emits the SPEC :143-156 threshold table per candidate.
- **`select_e1_winner(reports) -> E1CandidateReport`** -- the winner selection (promotion-by-loss guard, SPEC :69/:112): consults ONLY external-bench criteria (the threshold-table verdict; the human-QA soft verdict is a tiebreaker/advisory, NOT a hard gate per E-E8). The winner is the candidate that PASSES the full threshold table (absolute V4-orig >= 0.70 AND progression trending + latest >= 0.52 AND no_assist/exploit/mana_draw/parity/throughput/entropy/KL AND sanity gates); among multiple passers, the highest V4-orig H2H (the absolute anchor) wins; if NONE pass, E1 does NOT ship (surfaces a no-ship verdict -- the tournament can fail, this is the final gate).
- **Depends on:** `a_gate` (`compute_score_rate, check_*, evaluate_a_gate, select_promotion, play_gauntlet, GameRunner, EXPLOIT_AGENT_KINDS via gauntlet_v5` -- READ-ONLY), `second_start_parity` (`play_side_stratified_gauntlet, SecondStartParityLoop` -- READ-ONLY), `block_b_gate` (`_check_h2h_vs_best` pattern -- READ-ONLY reuse, NOT redefined), `opponents_v5` (`PHASE9_BROAD_OPPONENT_LANES` -- READ-ONLY), `rust_live_self_play` (`SelfPrevOpponent, V4MaxOpponent, the GameRunner wiring` -- READ-ONLY, the production runner is USER-provided at operational time; tests inject a fake GameRunner), `c_to_d_handoff.E1CandidateSet` (READ-ONLY consumption), `model_mlx.load_checkpoint` (READ-ONLY, E-E18). NO edit to `run_v5_acceptance.py` / `gauntlet_v5.py` / `league_v5.py` / `opponents_v5.py` / `a_gate.py` / `second_start_parity.py` / `block_b_*` (all READ-ONLY -- E3 is a NEW sibling composing them).
- **Constraints:** the two-notion separation (absolute vs V4-orig >= 0.70; progression vs best self-snapshot ~0.52-0.55 monotone, SPEC :138-141) is load-bearing -- E3 keeps them as DISTINCT gate rows; `league_v5.compare_adaptive_strength_monotonicity` is NOT used (synthetic proxy, `a_gate.py:33-36`); the retargeting to best self-snapshot REUSES Block B's `_DEFAULT_H2H_OPPONENT_KIND='best_ever'` (E3 does NOT retarget); the winner selection consults ONLY external-bench criteria.
- Tests: synthetic (fake `GameRunner` returning canned `GameResult` + a fake `candidate_loader` + a fake no_bonus JSON) -- the threshold-table verdict for a passing candidate (V4-orig 0.72, Random 0.97, end_turn 0.96, best-self trending [0.50,0.51,0.52,0.53,0.54] latest 0.54 >= 0.52, no_assist 0.56, exploit 0.51, mana_draw in band, gap 0.10, throughput 12500, entropy 0.72, kl 0.11, no_bonus p1 0.76/p2 0.71/second 0.71) = PASS; a candidate with V4-orig 0.68 = FAIL (absolute anchor); a candidate with best-self latest 0.51 = FAIL (progression floor); a candidate with Random 0.94 = FAIL (sanity); the winner selection picks the highest V4-orig among passers; no-passer = no-ship verdict; the two-notion separation is asserted (a candidate can pass progression but fail absolute, and vice versa); `league_v5.compare_adaptive_strength_monotonicity` is NOT called (regression guard).

### E4. `e1_human_qa_panel.py` -- human-QA panel driver (E-E8, load-bearing)
- **File:** NEW `TrainV3.5/python/train_v3/e1_human_qa_panel.py` (E-E8 = hybrid: in-worktree component + USER-run operational panel).
- **`HumanQAVerdict` (frozen dataclass)** -- the subjective-difficulty verdict schema: `candidate_path`, `n_battles`, `n_reviewers`, `mean_difficulty_score` (1-5 Likert, "subjectively harder for humans"), `n_harder_than_baseline` (vs the current prod baseline `extra-lr-v4-max`), `verdict` ('harder'/'comparable'/'easier'/'inconclusive'), `freeform_notes`, `stop_condition_met` (bool). This is the NEW subjective-difficulty capture (ZERO infrastructure exists today).
- **`E1HumanQAPanelDriver`** -- reuses the C2 deploy surface (`c2_collection_driver.C2CollectionDriver` + `V5RlhfAdapter`, READ-ONLY) to deploy each E1 candidate vs humans in rlhf_env; adds a post-battle reviewer scorecard capture (a reviewer submits a `HumanQAVerdict` row per candidate via an MCP tool / a JSON file the USER populates). The stop condition = `n_reviewers >= min_reviewers` AND `n_battles >= min_battles` (NOT C2's mana_draw-floor/battle-cap, which is a training-data objective -- E4's stop is a subjective-coverage objective).
- **`run_e1_human_qa_panel(candidates, *, c2_client, min_reviewers, min_battles) -> dict[candidate_path, HumanQAVerdict]`** -- the USER-run operational entry: deploys each candidate vs humans (via the C2 driver), collects the reviewer scorecards, emits the verdict map. The verdict is a SOFT gate (E-E8): a 'harder' verdict is a release-checklist item + a soft pass; an 'easier'/'inconclusive' verdict is a soft warn (NOT a hard fail -- the hard gates are the E3 threshold table).
- **Depends on:** `c2_collection_driver.C2CollectionDriver` (READ-ONLY, the deploy surface), `v5_rlhf_adapter.V5RlhfAdapter` (READ-ONLY, the V5 deploy adapter), `c_to_d_handoff.E1CandidateSet` (READ-ONLY). NO edit to `c2_collection_driver.py` / `v5_rlhf_adapter.py` (C1/C4 completed -- E4 composes them).
- **Constraints:** the panel is a SOFT gate (E-E8); the hard ship decision is the E3 threshold table + the E5 export-parity/fallback-guard; the rlhf_env deploy respects the prod-borrow-constraint (minimal-port, never full-refresh from webapp/); `V5RlhfAdapter.select_action` (`v5_rlhf_adapter.py:127-212`) handles the V5 3-tuple forward but DISCARDS `mana_draw_logit` (`:201` underscore-bound, argmaxes ONLY over the 601 logits, `:205-209`; `mana_draw_legal_mask`/`select_includes_mana_draw` are NEVER referenced in `v5_rlhf_adapter.py`) -- the rlhf_env deploy adapter is mana_draw-BLIND, so the E4 panel evaluates a V5 bot that never takes mana_draw actions. E4 reuses the adapter unchanged; the panel's "subjectively harder" verdict must be interpreted accordingly (the mana_draw axis is exercised only by the prod `_get_action_v5` path wired in E5, NOT by the panel).
- Tests: synthetic (a fake `c2_client` + a fake scorecard JSON) -- the verdict schema round-trips; the stop condition fires on `n_reviewers >= min` AND `n_battles >= min`; a 'harder' verdict is a soft pass; an 'easier' verdict is a soft warn (NOT a hard fail); the C2 deploy is invoked per candidate (spy); the verdict map keys are the candidate paths.

### E5. `e1_ship.py` -- ship component (E-E2, E-E11, E-E13, load-bearing)
- **File:** NEW `TrainV3.5/python/train_v3/e1_ship.py` + prod edits per E-E2.
- **`register_v5_kind_detector(registry=None)`** -- registers the V5 sidecar kind detector via `default_registry().register_detector(v5_detector)` (`policy_adapters.py:289`, LIFO over `_sidecar_kind_detector`). The detector keys off `model_version=='v5_split_encoder_onnx_v1'` OR (`obs_dim==7128` AND `mana_draw_head==true`) AND `format=='v5'` -> kind `'v5'`; returns None for V4 sidecars (V4 sidecars lack the V5 keys, so the V5 detector falls through and the LIFO chain proceeds to the V4 `_sidecar_kind_detector` which returns `'action_onnx'` -- the V5 detector returning None for V4 is the correct delegation, NOT a V4-clause rejection of V5). The V5 factory slot is ALREADY registered (`policy_adapters.py:411 _factory_v5_real`); E5 does NOT re-register the factory. This is SPEC :158 "register via `_derive_kind_from_sidecar`" (the `_sidecar_kind_detector` docstring at `:230` confirms the rename).
- **`ship_v5_winner(winner_report, *, onnx_export_fn, bundle_config) -> ShipResult`** -- the ship pipeline: (1) export the winner checkpoint to ONNX via E1 `export_v5_checkpoint_to_onnx` -> `ai/models/extra-lr-v5-max.onnx` + `.onnx.json` sidecar (E-E11 = max-only); (2) build the release bundle via `release_bundle.build_release_bundle` (REUSE AS-IS, `release_bundle.py:291` -- format-agnostic; the V5 onnx+sidecar land in the candidate dir); (3) register the V5 kind detector (above); (4) the PROD deploy wiring (E-E2): add `extra-lr-v5-max` to `BOT_MODEL_PROFILES` (`infrastructure/config.py:53-98`) -- `{model_path:'ai/models/extra-lr-v5-max.onnx', format:'v5', obs_dim:7128, action_feature_dim:171, max_candidate_actions:601, mana_draw_head:true, placement_mode:'append_only', verify_mask:False}`; retarget the top trophy tiers (`tier_hard_4500` `:194`, `tier_hard_plus_6000` `:205`, `tier_max_minus_7500` `:216`, + `:227`) `brain_profile` from `extra-lr-v4-max` to `extra-lr-v5-max` (V4-max stays as the lower-tier brain + asymmetric H2H reference); the `BerserkInference` V5 format branch + `_get_action_v5` path (E-E2 = option (a), the additive edit to `ai/bot_brain.py`).
- **`BerserkInference` V5 branch (E-E2, the prod edit)** -- in `ai/bot_brain.py`: (a) extend `_validate_*` to accept a V5 profile (`format=='v5'` -> validate `obs_dim==7128` + 3-tuple `outputs=['logits','value','mana_draw_logit']` + `mana_draw_head==true`; the V4 `_validate_train_v2_contract` stays unchanged for `format=='train_v2_classic_v1'`); (b) a NEW `_get_action_v5` path that reads the 3rd output `mana_draw_logit`, computes `mana_draw_legal_mask(state, player_id)` (`mana_draw_head_v5.py:76`, pure-Python engine-faithful), applies `select_includes_mana_draw(mana_draw_logit, best_candidate_logit, mana_draw_legal)` (`:116`) to decide between the 601-best and the `ManaDrawAction`, and argmax-decodes the 601-best via the existing `decode_action` path. The V4 `_get_action_train_v2_classic` path (`:407-521`) is UNCHANGED. (c) a logits-finite / argmax-legal guard (SPEC :174, the ONNX fallback guard for V5): if the V5 ONNX produces NaN/garbage logits OR no masked-in candidate, raise RuntimeError (NOT a silent rule-based fallback -- the last-resort PROD safety).
- **`ShipResult`** -- `{winner_path, onnx_path, sidecar_path, bundle_dir, manifest_path, marker:'extra-lr-v5-max', prod_profile_key, trophy_tiers_retargeted, fallback_guard_verified}`.
- **Depends on:** E1 `export_v5_checkpoint_to_onnx`, E2 (parity pass + fallback-guard verified -- the ship is GATED on E2 green), E3 `select_e1_winner` (the winner_report), E4 `HumanQAVerdict` (soft), `release_bundle.build_release_bundle` + `discover_release_inputs` (REUSE AS-IS, `release_bundle.py:106,291`), `policy_adapters.default_registry/register_detector` (READ-ONLY), `mana_draw_head_v5.mana_draw_legal_mask/select_includes_mana_draw` (READ-ONLY, wired into `_get_action_v5`). PROD EDITS (E-E2): `ai/bot_brain.py` (additive V5 branch), `infrastructure/config.py` (`BOT_MODEL_PROFILES` + top trophy tiers).
- **Constraints (PROD SAFETY, surfaced explicitly):** (1) the `ai/bot_brain.py` + `infrastructure/config.py` edits are LIVE prod files (`web/server.py:5745` loads `BERSERK_BRAIN` for every live bot turn) -- the edit MUST be additive (new `format='v5'` branch + new `_get_action_v5` + new profile key + retarget ONLY the top tiers) and MUST NOT change V4 behavior or the trophy->brain_profile mapping for non-top tiers; (2) the ONNX fallback guard (SPEC :174) is the last-resort prod safety -- a V5 ONNX that loads but produces NaN/garbage MUST raise RuntimeError, NOT silently degrade to a rule-based policy; (3) the `_get_action_v5` path MUST wire `mana_draw_legal_mask` + `select_includes_mana_draw` -- shipping a V5 ONNX with a mana_draw head WITHOUT wiring it would silently drop mana_draw (the head output ignored, model plays like a 601-only V5) -- a ship-correctness blocker; (4) the V5 sidecar `model_version` is distinct from the V4 string BUT a distinct `model_version` alone does NOT prevent the V4 `_sidecar_kind_detector` (`policy_adapters.py:219-248`) from matching the V5 sidecar via the `inputs/action_feature_dim` OR-branches (`:240-241`) -- V5 routing relies on the LIFO V5 detector (`register_detector`, `:289`) returning `'v5'` (never None); a regression test asserts this (LIFO load-bearing-tested); (5) NO TrainV3.5-into-prod import AND NO prod->rlhf_env import -- the prod `_get_action_v5` path uses `mana_draw_head_v5` (TrainV3.5) -- verify the import boundary (`mana_draw_head_v5.py:41-42` imports ONLY `core.engine`/`core.state`, prod-core, NO TrainV3.5-internal deps, so it vendors cleanly into `ai/` directly). The SOLE vendoring target is `ai/` (prod); the `rlhf_env/components/, which prod can import` alternative is STRUCK -- grep over `ai/`/`web/`/`infrastructure/` returns ZERO `import rlhf_env`/`from rlhf_env` hits (prod has no rlhf_env imports; E-E2 (c) was rejected for exactly this cross-module dependency). Note `offline_dataset_loader.py:113-115` does import `train_v3.mana_draw_head_v5`, but that is the OFFLINE training bridge (NOT the live hot path: `web/server.py` never references `offline_dataset_loader`), so it does not establish a live-path import; (6) `BOT_DIFFICULTY_PROFILES` (`config.py:243-263`) is DERIVED from `BOT_MODEL_PROFILES` keyed by tier `brain_profile` -- verify the derivation propagates the new `extra-lr-v5-max` key (the retargeted tiers pick it up automatically); (7) the ship is GATED on E2 (parity + fallback-guard) green + E3 (winner passes the threshold table) -- E1 does NOT ship a failing candidate.
- Tests: synthetic (a fake ONNX blob + a fake `BerserkInference` V5 branch + a fake release bundle dir) -- the V5 kind detector returns `'v5'` for a V5 sidecar and None for a V4 sidecar; `ship_v5_winner` exports + bundles + registers + wires the prod profile; the `BerserkInference` V5 branch accepts `obs_dim=7128` + 3-tuple and REJECTS a malformed V5 ONNX (NaN logits -> RuntimeError, the fallback guard); the `_get_action_v5` path applies `select_includes_mana_draw` (mana_draw taken iff legal AND logit > best 601 logit); the V4 `_get_action_train_v2_classic` path is unchanged (regression guard); the top trophy tiers retarget to `extra-lr-v5-max` and the non-top tiers stay `extra-lr-v4-*` (regression guard); `BOT_DIFFICULTY_PROFILES` derivation includes `extra-lr-v5-max`. The prod `ai/bot_brain.py` edit is tested with a fake `ort.InferenceSession`.

---

## 4. Frozen-classic guard (preserved, in effect -- carries from Block A/B/C/D)

- **Prod runtime must not import TrainV3.5 AND must not import rlhf_env** -- all E1/E2/E3/E4 components live under `TrainV3.5/python/train_v3/`; the ONLY prod-touching artifacts are (a) `ai/bot_brain.py` (the additive V5 branch, E5) + `infrastructure/config.py` (the new profile key + top-tier retarget, E5) + (b) the V5 kind detector registered into `rlhf_env/components/policy_adapters.py` (the worktree's tracked copy). The prod `_get_action_v5` path uses `mana_draw_head_v5` -- `mana_draw_head_v5.py:41-42` imports ONLY `core.engine`/`core.state` (prod-core, NO TrainV3.5-internal deps), so it vendors cleanly into `ai/` directly (the SOLE vendoring target; `rlhf_env/components/` is NOT a prod-importable target -- prod has ZERO `rlhf_env` imports, E-E2 (c) was rejected for this cross-module dependency) -- surfaced as a constraint in E5, verified at execution.
- **Frozen-classic byte-frozen** -- `classic_obs_v1`/`classic_actions_v1`/`classic_card_shape_v1`/`classic_rl_env.py`/`reward_v5.py` NOT touched; the V5 exporter emits `action_features` dim 171 + 601 logits (the frozen candidate block); `mana_draw` is a PARALLEL binary head, NEVER a 602nd candidate.
- **V4-orig ONNX runs UNCHANGED forever (SPEC gamma)** -- `ai/train_v2/export_onnx.py`, `ai/train_v2/onnx_policy.py`, `ai/train_v2/model_mlx.py`, the existing `extra-lr-v4-*.onnx` files, the `BOT_MODEL_PROFILES` v4 keys, and the `BerserkInference` V4 path (`_validate_train_v2_contract` + `_get_action_train_v2_classic`) MUST NOT be broken. The V5 exporter is a NEW file (E1); the V5 prod branch is additive (E5).
- **`v5_trace.py` NOT modified + NOT imported** into the E1 export/ship path (the export reads the V5 npz, NOT v5_trace; the ship deploys via ONNX, NOT v5_trace).
- **`core/state.py` NOT modified**; `league_v5.py`/`gauntlet_v5.py`/`opponents_v5.py`/`a_gate.py`/`second_start_parity.py`/`block_b_*`/`snapshot_pool.py` consumed READ-ONLY (E3 composes them, does not edit); `run_v5_acceptance.py`/`run_v5_vs_v4max_benchmark.py` READ-ONLY (their threshold CLI defaults are reused as constants where they match the spec; the broken `TrainV3` sys.path is NOT fixed in-place -- E3 is a new sibling).
- **`rust_ffi.py` / Rust NOT touched** -- E1 makes ZERO `.rs`/`.toml` edits (the live H2H candidate-vs-snapshot runs through the PYTHON A4 `rust_live_self_play` runner driving `BatchedRolloutWorker` via FFI, `worker.rs:485` -- NO Rust candidate-policy hook). If Rust is touched, re-confirm 157 cargo tests green (not expected).
- **`rlhf_env/components/*.py` tracked in this worktree** (D-C0 carry) -- edits to `policy_adapters.py` (the V5 detector registration) are recoverable via `git checkout`; the C1 `v5_rlhf_adapter.py` / `c2_collection_driver.py` are NOT edited by E4 (composed READ-ONLY).
- **Warm-start Q3 PARTIAL relaxation is BINDING** -- E2 honors it (no 601-bit-close; faithful-layer equality + base-1456 path identity + the faithful LAYER-output bit-close bonus [NOT a forward-pass-to-logits bit-close -- the fresh `state_fuser.layers.0` makes a base-1456-only forward to the 601 logits diverge >0.05], E-E14).
- **Promotion-by-loss guard** -- the E3 winner selection consults ONLY external-bench criteria; internal PPO loss/KL/entropy are monitoring-only (SPEC :69/:112).
- **ONNX fallback guard (SPEC :174, the last-resort PROD safety)** -- the V5 deploy path (`V5RlhfAdapter` + the prod `_get_action_v5`) raises RuntimeError on a silent rule-based fallback / NaN/garbage logits; E2 verifies it; E5 wires it.

---

## 5. Open questions / latent findings carried

1. **E-E2 (THE prod deploy path)** -- extend `BerserkInference` (V5 branch + `_get_action_v5`) vs port `V5RlhfAdapter` to `web/server.py` vs use the rlhf_env registry. The largest E1 design choice; default = (a) extend BerserkInference (additive, V4 unchanged).
2. **E-E3 (tightened LOGIT_TOL)** -- 1e-4 measure-then-pin vs 1e-5 vs 5e-4. The V5 fused trunk MAY drift more than V4's ~1e-8; the measure-then-pin probe (E2) settles it data-driven. Default = 1e-4.
3. **E-E5 (progression absolute floor)** -- hard 0.52 gate vs calibration target. Default = hard 0.52 (trending AND latest >= 0.52).
4. **E-E6 (no_bonus retarget)** -- retarget the no_bonus gate to best self-snapshot (SPEC :149, >= 0.70 each, hard) + V4-max no-bonus secondary advisory vs keep V4-max as the gate. Default = retarget to self-snapshot (SPEC-literal) + V4-max advisory.
5. **E-E8 (human-QA panel)** -- in-worktree component vs USER-run vs hybrid. Default = hybrid (soft gate).
6. **E-E10 (ship artifact format)** -- ONNX 3-output (head wired) vs 601-only vs MLX npz. Default = ONNX 3-output.
7. **E-E14 (warm-start ship-gate scope)** -- faithful-layer + base-1456 path identity + faithful LAYER-output bit-close bonus (NOT forward-to-logits) vs strict Q3 PARTIAL. Default = faithful + bonus.
8. **V5 Metal-vs-CPU drift magnitude (latent, E-E16)** -- the ~1e-8 drift comment is for the V4 net; V5's larger fused trunk MAY drift more. The E2 measure-then-pin probe resolves this before pinning `LOGIT_TOL_V5`.
9. **`mana_draw_head_v5` prod import boundary (latent, E5 constraint 5)** -- `mana_draw_head_v5.py` imports `core.engine`/`core.state` (NOT TrainV3.5), but it LIVES under `TrainV3.5/python/train_v3/`. The prod `_get_action_v5` path (in `ai/bot_brain.py`) must NOT import TrainV3.5 -- so `mana_draw_head_v5` (or just the two pure-Python helpers `mana_draw_legal_mask` + `select_includes_mana_draw`) must be vendored into prod or `rlhf_env/components/`. Surfaced for execution-time verification.
10. **`BOT_DIFFICULTY_PROFILES` derivation (latent, E5 constraint 6)** -- `config.py:243-263` derives `BOT_DIFFICULTY_PROFILES` from `BOT_MODEL_PROFILES` keyed by tier `brain_profile` + aliases. Adding `extra-lr-v5-max` to `BOT_MODEL_PROFILES` + retargeting the top tiers SHOULD propagate automatically -- verify at execution.
11. **`run_v5_vs_v4max_benchmark.py` broken path (latent)** -- the V4-max benchmark script has the SAME broken `TrainV3` sys.path (`:26`) as `run_v5_acceptance.py:16`. E3 does NOT fix it in-place (READ-ONLY); if E-E4 = live H2H, the pre-baked JSON path is secondary and the broken script is not on the E1 critical path. Flagged.
12. **D-D6 short-D veto (confirm-only, E-E19)** -- the short-D schedule that produced post-D is an implementer default surfaced as confirm-only; the user may veto at E1-prep time. Default = confirm.

---

## 6. Dependency graph

```
Block A + B + C + D in-worktree COMPLETE (A4 rust_live_self_play, A5 a_gate, B1 snapshot_pool,
  B3/B5/B6/B7/B8, C1 v5_rlhf_adapter + c2_collection_driver, C4 c_loop_driver,
  D1/D2/D3 block_d_opponent_mix/block_d_league_driver/c_to_d_handoff [E1CandidateSet threaded])
   |
   +-- E1 export_onnx_v5.py (V5 ONNX exporter: TorchV5ActionConditionedPolicy + V5_WEIGHT_MAP
   |     + 3-tuple output + V5 sidecar; NEW file, V4 export_onnx.py untouched)
   |     depends: v5_policy.V5ActionConditionedPolicy (:29 RO mirror), contracts.OBS_V5_DIM (:36 RO),
   |              model_mlx.save/load_checkpoint (:89/:117 RO npz format), v5_card_shape_v1.CARD_SHAPE_VERSION (RO),
   |              design.md:40/158/186/189, E-E1/E-E10/E-E13
   |
   +-- E2 test_export_onnx_v5.py (V5 export-parity + integrity: tightened LOGIT_TOL_V5 + mana_draw_logit parity
   |     + warm-start faithful-layer re-assert + base-1456 faithful LAYER-output bit-close bonus + ONNX fallback guard)
   |     depends: E1 export_v5_checkpoint_to_onnx, v5_policy, mana_draw_head_v5 (:76/:116 RO),
   |              warm_start_v5.load_v4_max_into_v5 (:176 RO), v5_rlhf_adapter.V5RlhfAdapter (RO),
   |              tests/test_train_v2_warm_start_v5.py (:122-268 RO template), design.md:174-176/189, E-E3/E-E14
   |
   +-- E3 e1_tournament.py (tournament harness + final-acceptance gate: Rust gauntlet over the 3 E1CandidateSet
   |     candidates, retargeted to best self-snapshot [REUSES Block B _DEFAULT_H2H_OPPONENT_KIND='best_ever']
   |     + V4-orig absolute >=0.70 + Random/end_turn sanity >=0.95 + the SPEC :143-156 threshold table
   |     + two-notion winner selection [external-bench only])
   |     depends: a_gate (compute_score_rate/check_*/evaluate_a_gate/select_promotion/play_gauntlet/GameRunner RO),
   |              second_start_parity (play_side_stratified_gauntlet/BLOCK_B_GAP_THRESHOLD RO),
   |              block_b_gate (_check_h2h_vs_best pattern RO), opponents_v5 (PHASE9_BROAD_OPPONENT_LANES RO),
   |              rust_live_self_play (SelfPrevOpponent/V4MaxOpponent RO; production GameRunner USER-provided),
   |              c_to_d_handoff.E1CandidateSet (:61 RO consumption), model_mlx.load_checkpoint (RO, E-E18),
   |              run_v5_acceptance.py (:33-45 threshold CLI defaults RO), design.md:135-156, E-E4..E-E9/E-E17
   |
   +-- E4 e1_human_qa_panel.py (human-QA panel driver: C2 deploy surface reuse + HumanQAVerdict subjective schema
   |     + soft gate; USER-run operational panel)
   |     depends: E3 select_e1_winner (the winner), c2_collection_driver.C2CollectionDriver (RO),
   |              v5_rlhf_adapter.V5RlhfAdapter (RO), c_to_d_handoff.E1CandidateSet (RO),
   |              design.md:135/156, E-E8
   |
   +-- E5 e1_ship.py (ship: V5 kind detector registration + release_bundle.build_release_bundle composition
         + prod deploy wiring [BerserkInference V5 branch + _get_action_v5 + BOT_MODEL_PROFILES extra-lr-v5-max
         + top trophy tier retarget] + ONNX fallback guard verify)
         depends: E1 export_v5_checkpoint_to_onnx, E2 (parity + fallback-guard GATED green),
              E3 select_e1_winner (winner_report), E4 HumanQAVerdict (soft),
              release_bundle.build_release_bundle/discover_release_inputs (:106/:291 REUSE AS-IS),
              policy_adapters.default_registry/register_detector (:289/:404 RO; V5 factory slot :411 ALREADY registered),
              mana_draw_head_v5.mana_draw_legal_mask/select_includes_mana_draw (:76/:116 RO, wired into _get_action_v5),
              PROD EDITS: ai/bot_brain.py (additive V5 branch), infrastructure/config.py (BOT_MODEL_PROFILES + top tiers),
              design.md:158/174/186, E-E2/E-E11/E-E13
```

**Execution order:** E1 (exporter) -> E2 (parity test, needs E1) -- export track; E3 (tournament) -- eval track (independent of E1/E2, parallel-able); E4 (human-QA, needs E3 winner); E5 (ship, needs E1 + E2 green + E3 winner + E4 soft verdict) LAST. The operational tournament+ship RUN (the `block_e1_runner.py` thin wrapper invoking E3 -> E4 -> E1 -> E5 on a real `BlockDLeagueManifest.candidate_paths` with real MLX/Rust) is USER-run glue gated on the D-league RUN producing the candidate set -- the in-worktree components are synthetic-tested regardless (E-E12 = hybrid).

DURABLE: write ASCII-only workflow scripts to `.claude_tmp_e1_{N}.js`, `node --check`, invoke via `scriptPath`, delete temp before commit. No function declarations, no backtick template literals, no string-concat prompts (the nested-quote hazard that broke D2's first parse).

## 7. Execution (ultracode, each: implementer + 4 refute-by-default verifiers + fix; independently re-confirm green; commit; push)

Solo-draft + verify-only for plan-writers (the stalling fix from Block B, durable since Block C/D). This plan is solo-drafted from the 6-agent grounding fan-out above; the verify-only workflow (4 refute-by-default critics + Finalize) runs next to merge findings surgically BEFORE the E-E* user decisions are presented via AskUserQuestion. Then each component E1 -> E2 -> E3 -> E4 -> E5 is implemented via the implementer + 4-verifier + fix ultracode workflow, independently re-confirmed green, committed, pushed. BLOCK_E1_COMPLETION.md + the ship (USER-run operational deploy of `extra-lr-v5-max` to prod) after.

**Operational-vs-component split (the prior-block pattern):** the in-worktree components E1-E5 are synthetic-testable (fake ONNX / fake InferenceSession / fake GameRunner / fake C2 client -- no real MLX/Rust/ONNX/rlhf_env required for tests, skip-gated where MLX/onnxruntime needed). The operational tournament+ship RUN (real MLX/Rust games + the real prod deploy) is USER-run glue gated on the D-league RUN producing a `BlockDLeagueManifest.candidate_paths` -- the same operational pattern as Block C (C-loop RUN USER-run per D-C0/D-C9) and Block D (D-league RUN USER-run per `BLOCK_D_COMPLETION.md:106`). The thin `block_e1_runner.py` wrapper (E-E12 = hybrid) is the operational entry the USER invokes; it is synthetic-tested with fakes, the real RUN is USER-executed. The prod deploy (the `ai/bot_brain.py` V5 branch + the `extra-lr-v5-max` profile + the trophy-tier retarget) is a LIVE prod edit -- the E5 component builds + synthetic-tests it; the actual prod deploy (loading the new `BERSERK_BRAIN` with the V5 profile) is USER-run with a prod-safety checkout (the ONNX fallback guard is the last-resort, SPEC :174).