<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.BLUETOOTH"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_CONNECT"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        tools:node="remove" />

    <application>
        <meta-data
            android:name="sdk_pay_scheme_value"
            android:value="${sdk_pay_scheme_value}" />
        <meta-data
            android:name="console_app_id_value"
            android:value="${console_app_id_value}" />
        <meta-data
            android:name="ru.rustore.sdk.pay_scheme"
            android:value="${sdk_pay_scheme_value}" />
        <meta-data
            android:name="ru.rustore.sdk.console_app_id"
            android:value="${console_app_id_value}" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="${sdk_pay_scheme_value}" />
            </intent-filter>
        </activity>
    </application>
</manifest>
