# Runtime dependencies for the web app, Telegram bot, payments, push
# notifications, and ONNX inference. TrainV/MLX dependencies are intentionally
# not included here because they are not needed on the Orange Pi deployment host.

aiohttp==3.9.1
aiogram==3.22.0
aiosmtplib==3.0.2
asyncpg==0.30.0
bcrypt==4.3.0
certifi==2025.8.3
cryptography==46.0.5
firebase-admin==7.4.0
numpy==2.3.2
onnx==1.21.0
onnxruntime==1.22.1
pillow==12.1.1
PyJWT==2.11.0
python-dotenv==1.0.1
python-socketio==5.11.1
requests==2.32.5
yookassa==3.0.0
