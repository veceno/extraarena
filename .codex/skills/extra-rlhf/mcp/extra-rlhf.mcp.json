{
  "_comment": "ExtraRLHF MCP server registration. Pin both the dependency-bearing Python interpreter and exact checkout/worktree. The server is stdio JSON-RPC 2.0 and must run from the repo root.",
  "mcpServers": {
    "extra-rlhf": {
      "command": "<PYTHON_WITH_RLHF_DEPS>",
      "args": [
        "-m", "rlhf_env.mcp_server",
        "--models-dir", "ai/models",
        "--sessions-dir", "rlhf_env/sessions",
        "--cards-path", "ai/cards.json"
      ],
      "cwd": "<REPO_ROOT>"
    }
  }
}
